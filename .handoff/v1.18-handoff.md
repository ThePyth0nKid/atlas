# V1.18 — Agent Handoff

You are continuing the Atlas Verifiable Knowledge Graph project. **V1.18 Welle B (1) SHIPPED (2026-05-06) — `docs/CONSUMER-RUNBOOK.md` §10 "Sigstore Public Good incident — extended degradation protocol" is live** (PR #7, squash-merged into `master` as commit `a7e01ed`, +343 lines pure-docs). Trigger C from ADR-Atlas-006 §4 is **now actionable** — the documentation gap explicitly recorded inside the ADR is closed. **V1.18 Welle A SHIPPED earlier the same day — ADR-Atlas-006 "Multi-Issuer Sigstore Tracking" is live as `docs/ADR/ADR-Atlas-006-multi-issuer-sigstore-tracking.md`** (PR #5, squash-merged into `master` as commit `c6aed22`). The ADR is the first under `docs/ADR/` and establishes the directory + the format. **Status field: Tracking (not adopted).** ADR documents Atlas's three structurally distinct Sigstore touchpoints (A: Rekor v1 anchor verification in `crates/atlas-trust-core/src/anchor.rs`; B: `npm publish --provenance` in `wasm-publish.yml`; C: `npm audit signatures` round-trip in `verify-wasm-pin-check@v1` Layer 3), the upstream multi-issuer landscape (Rekor v2, npm-side TUF root rotation, `tuf-conformance`, AWS-hosted Sigstore, Sigstore Foundation governance), four concrete adoption triggers (npm trust root second issuer, Rekor-v2-issued attestation, Sigstore Public Good extended incident, Atlas-side compliance requirement), and the explicit non-decision: track upstream, do not pre-adopt, until at least one trigger fires. Decision rationale: cost of premature adoption (schema lock-in, alpha APIs, divergent verification semantics across consumers) outweighs marginal risk reduction from joining a federation that is itself not yet stable. Reversibility documented (§8.4 of ADR): adoption work is prepared (§5), upstream tracking is documented (§7), trigger conditions are concrete (§4); no technical debt is being incurred, only ongoing tracking effort, deliberately scoped to quarterly cadence.

V1.17 (Welle A + Welle B + Welle C) remains the current trust posture for downstream-consumable releases — see `.handoff/v1.17-handoff.md` for the full V1.17 history. V1.17 Welle A: `verify-wasm-pin-check@v1` Auto-Verify CI Composite Action (40/40 fixtures, 4-job self-test workflow against npmjs.org + rekor.sigstore.dev). V1.17 Welle B: SSH-based tag-signing enforcement (14/14 anti-drift fixtures, end-to-end-validated against the live `v1.17.0` tag). V1.17 Welle C: Trust-Root Mutation Defence (17/17 anti-drift fixtures, structurally bound via Repository Ruleset `Master trust-root protection` with `bypass_actors: []`). Tag `v1.17.0` is LIVE at commit `d955567` — signed by SSH ed25519 key SHA256:qq/VVJYpsgEdeQSLqU0QS/gKn6ohXJHio+VkzVX+4Zg, verified green by both CI gates (`verify-tag-signatures` run 25395355143 + `wasm-publish` run 25395355145).

The user prefers **German** in chat; respond in German. Standing directive: *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Entscheide du, was jetzt das Bestmögliche ist anhand deiner Informationen, die du hast. Immer beste Sicherheit und beste Codequalität."*

> ⚠️ **Read this in full before touching code.** It captures the V1.18 Welle B (1) scope rationale (why CONSUMER-RUNBOOK §10 was the highest-leverage Welle B follow-on; why the protocol uses an Atlassian-Statuspage-JSON + GitHub-security-advisories + out-of-band-signals triangulation rather than a single status check; why "Sigstore + allowed_signers mutation" is a designated hard-pause row in the escalation table), the V1.18 Welle B (1) parallel-review correction record (5 CRITICAL + 8 HIGH applied in-doc before commit), the V1.18 Welle A scope rationale (why one ADR for three touchpoints; why "tracking, not adopting" is the deliberate non-decision; why §5.3 `SIGSTORE_DEGRADED_OK` is documented as an open problem rather than a quick fix), the V1.18 Welle A parallel-review correction record (3 CRITICAL + 3 HIGH applied in-doc before commit), and the V1.18 Welle B (2+) / V1.19+ candidate list now that Welle B (1) is closed.

> **If you only read three things:** (1) "Session opener — start here" right below — the recommended next action; (2) §0 "V1.18 Welle B (1) — CONSUMER-RUNBOOK §10 Sigstore Incident Protocol" — what shipped + parallel-review correction record + Trigger-C-actionability note; (3) §1 "V1.18 Welle A — Multi-Issuer Sigstore Tracking ADR" — what shipped earlier the same day + parallel-review correction record + recorded follow-ons. Skip the rest unless you need provenance for a specific design decision.

> **Doc structure:** §0 = V1.18 Welle B (1) CONSUMER-RUNBOOK §10 Sigstore Incident Protocol (current — READ). §1 = V1.18 Welle A Multi-Issuer Sigstore Tracking ADR (also current — READ). §"V1.17 sign-off" = V1.17 fully-closed pointer (audit-only — see `.handoff/v1.17-handoff.md` for full V1.17 history). §"Standing protocol" = workflow rules (re-read each session).

---

## Session opener — start here

**Working tree state (as of 2026-05-06, after V1.18 Welle B (1) SHIPPED):** clean on `master`, in sync with `origin/master`. No uncommitted changes. No stashes. PR #7 squash-merged at commit `a7e01ed`; branch `docs/v1.18-welle-b-1-consumer-runbook-sigstore-incident-protocol` deleted local + remote. Top of `git log --oneline -5`: `a7e01ed docs(v1.18/welle-b): CONSUMER-RUNBOOK §10 — Sigstore Public Good incident protocol (#7)` → `e9fe923 docs(v1.18/handoff): V1.18 Welle A SHIPPED — opens the V1.18 handoff (#6)` → `c6aed22 docs(v1.18/welle-a): ADR-Atlas-006 — Multi-Issuer Sigstore Tracking (#5)` → `f6de592 docs(v1.17/handoff): V1.17.0 SHIPPED` → `d955567 fix(v1.17/welle-b): refetch annotated tag objects after checkout — CI partial-clone gap`. **`verify-trust-root-mutations` PASS** on PR #7 (5 s) — re-confirms pure-docs PRs outside `PROTECTED_SURFACE` flow through the V1.17 Welle C gate as a no-op (the runbook is documentation, not a trust-root file).

**Verify-before-touch checklist** (run these to confirm baseline before any V1.18 Welle B / V1.19 edit):
- `bash tools/test-trust-root-mutations.sh` → exit 0, `PASS: 17 / 17` (V1.17 Welle C anti-drift gate).
- `bash tools/test-tag-signatures.sh` → exit 0, `PASS: 14 / 14` (V1.17 Welle B anti-drift gate).
- `bash .github/actions/verify-wasm-pin-check/test/run-tests.sh` → exit 0, `PASS: 40 / 40` (V1.17 Welle A anti-drift gate).
- `bash tools/playground-csp-check.sh` → exit 0, `PASS`, `frame-ancestors` WARN + `report-uri: /csp-report` line.
- `cd apps/wasm-playground/worker && npx vitest run` → 91 passing across 4 test files. `npx tsc --noEmit` → exit 0.
- `/c/Users/nelso/.cargo/bin/cargo.exe test --features hsm --workspace` → 347 passing.
- `git status` → clean; `git log --oneline -5` → top should be the V1.18 Welle B (1) `docs(v1.18/welle-b)` commit (`a7e01ed`), then the V1.18 Welle A handoff opener (`e9fe923`), then the V1.18 Welle A `docs(v1.18/welle-a)` commit (`c6aed22`), then the V1.17 Welle B handoff + CI fix pair.
- ADR sanity-read: `head -20 docs/ADR/ADR-Atlas-006-multi-issuer-sigstore-tracking.md` → status field `Tracking (not adopted)`, date `2026-05-06`.
- CONSUMER-RUNBOOK sanity-read: `grep -n '^## 10\.' docs/CONSUMER-RUNBOOK.md` → `## 10. Sigstore Public Good incident — extended degradation protocol` plus seven `## 10.N` subsection headers (10.1 Why a separate protocol → 10.7 Explicit non-goals).

**Recommended next action (default if user says "weiter" / "go" / "next"):**

→ **Open V1.18 Welle B (2) with one of the prioritised candidates.** V1.18 Welle B (1) (CONSUMER-RUNBOOK §10) is now closed; the V1.18 Welle A ADR-006 follow-ons (§5.1, §5.2, §5.3) remain the natural next steps. Highest-leverage candidates, in priority order: **(1) `feat(v1.18/welle-b)` anchor.rs RekorIssuer registry refactor (ADR-006 §5.1)** — replace the top-level `SIGSTORE_REKOR_V1_PEM` + `SIGSTORE_REKOR_V1_ACTIVE_TREE_ID` + `SIGSTORE_REKOR_V1_TREE_IDS` constants with a `RekorIssuer { name, pem, active_tree_id, tree_id_roster }` struct + `&[&RekorIssuer]` slice. Mechanical, no semantics change, lowers future Rekor-v2-adoption cost, makes the `sigstore_tree_id_roster_is_pinned` invariant test per-issuer. Touches `crates/atlas-trust-core/src/anchor.rs` — IS in `PROTECTED_SURFACE`, so signed-by-allowed-signer commit required. **(2) `docs(v1.18/welle-b)` OPERATOR-RUNBOOK §15 inline-pin-update protocol (ADR-006 §5.2)** — document the protocol for updating `SIGSTORE_REKOR_V1_PEM` and the tree-ID array (PR review requirements, golden-test fixture regeneration, cross-version-anchor compatibility test). Currently implicit; documenting makes the update path auditable. CONSUMER-RUNBOOK §10.6 closure step (3) explicitly references this section as TBD — shipping this closes one of two TBDs created by Welle B (1). Pure-doc, autonomous-friendly. **(3) `docs(security-notes)/scope-k-forward-link-adr-006`** — small docs PR adding a forward reference from `SECURITY-NOTES` scope-k to ADR-006 (the MED finding from V1.18 Welle A parallel review, recorded as a follow-on rather than blocking the ADR ship). **(4) Sigstore/gitsign tag-signing as additive backup channel** (V1.17 leftover) — keep SSH as primary trust root, add gitsign-signed tags as a parallel verifiable channel; defers until Sigstore Windows tooling matures. **(5) Action consumer-side adoption proof** (V1.17 leftover) — write a downstream-repo demo consuming `verify-wasm-pin-check@v1.17.0` via SHA-pin, demonstrating green CI. **(6) MCP-Verifier-Hook RFC** (long-horizon, ADR-Atlas-005 V2-Roadmap). Confirm scope with the user in one sentence before starting.

**Workflow reminder:** for any code edits going forward, follow the standing protocol — implement → parallel `code-reviewer` + `security-reviewer` → fix CRITICAL/HIGH/selected MEDIUMs in-commit → single `feat(v1.18/...)` or `docs(v1.18/...)` commit + `docs(v1.18/handoff)` commit prepending a new status block above §0 in this file → push to `origin/master`. Anti-drift gates: `bash tools/test-trust-root-mutations.sh` (V1.17 Welle C), `bash tools/test-tag-signatures.sh` (V1.17 Welle B), `bash .github/actions/verify-wasm-pin-check/test/run-tests.sh` (V1.17 Welle A), `bash tools/playground-csp-check.sh` (V1.16). Cargo PATH on Windows is non-default — the repo is a Windows host with bash, so all shell snippets here use MSYS forward-slash paths. **Trust-root surface awareness**: any edit touching the ten files in `PROTECTED_SURFACE` or the `.github/actions/verify-wasm-pin-check/` subtree triggers `verify-trust-root-mutations.yml` and requires a commit signed by a key in the *prior* `.github/allowed_signers`. Pure-docs changes outside that surface (ADRs, runbooks, this handoff file) bypass the gate cleanly — V1.18 Welle A confirmed this with a 5-second PASS.

**Before you touch anything**, confirm scope with the user in one sentence, then proceed under the standing directive.

---

## 0. V1.18 Welle B (1) (2026-05-06) — CONSUMER-RUNBOOK §10 Sigstore Public Good Incident Protocol (current)

**State summary:** the documentation gap recorded inside ADR-Atlas-006 §4 Trigger C is closed. `docs/CONSUMER-RUNBOOK.md` now contains a §10 "Sigstore Public Good incident — extended degradation protocol" with seven sub-sections (§10.1–§10.7, +343 lines pure-docs). PR: #7 (squash-merged as `a7e01ed`). Date: 2026-05-06. Wave: V1.18 Welle B (1). Status: `verify-trust-root-mutations` PASS in 5 s on PR #7 — re-confirms pure-docs PRs outside `PROTECTED_SURFACE` flow through the V1.17 Welle C gate cleanly.

**Why this was the highest-leverage Welle B follow-on:** ADR-Atlas-006 §4 Trigger C explicitly stated *"CONSUMER-RUNBOOK has no §10 'Sigstore Public Good incident — extended degradation protocol' section. Filling this gap is an explicit prerequisite for Trigger C readiness."* Without §10, an actual Trigger C event (Sigstore Public Good ≥24h availability/integrity loss, unscheduled pubkey rotation, documented integrity breach) would force ad-hoc operator communication — exactly the failure mode an incident runbook is supposed to prevent. **Trigger C is now actionable**; Atlas can respond to a Sigstore Public Good incident with a documented decision tree instead of improvisation.

**Seven sub-sections shipped (§10.1–§10.7):**

- **§10.1 — Why a separate protocol.** Clarifies that the existing CONSUMER-RUNBOOK §1–§9 covers three online action layers (npm fetch, `npm audit signatures`, `verify-wasm-pin-check@v1`), V1.17 Welle B SSH-tag-signing covers the publish-side trust root, and §6 covers the offline reproduce-from-source fallback. §10 adds a fourth axis: protocol for sustained Sigstore Public Good degradation that is too long for a single CI re-run to absorb but stops short of permanent infrastructure migration.
- **§10.2 — Trigger C classification + verification path.** Three-source triangulation defending against `sigstore.dev` DNS-authority hijack (Atlassian Statuspage JSON API at `https://www.sigstoreusergroup.io/api/v2/summary.json` for machine-readable status; GitHub security-advisory API at `api.github.com/repos/sigstore/{rekor,fulcio,sigstore}/security-advisories` for incident communication; out-of-band signals like Sigstore Foundation public mailing lists and ≥2 independent maintainer accounts on different social platforms — these are deliberately fate-NOT-shared with the Sigstore-domain DNS chain).
- **§10.3 — 4-row decision tree.** Per-row: prerequisites + run-command + audit-log requirements. Rows: (a) no-new-install lane (`npm ci --ignore-scripts` re-integrity prerequisite; existing locked deps continue working); (b) lockfile-bump lane (crates.io pre-cache mitigation; cargo-vendor or in-tree vendored crate path); (c) fresh-clone lane (downstream-consumer who never had a working baseline — most degraded class, requires waiting OR §6 fallback); (d) re-run audit lane (`npm audit signatures` against archived attestations, NOT against the live Rekor).
- **§10.4 — 7-field audit-log entry.** Fields: incident_id, status_snapshot (with `curl https://www.sigstoreusergroup.io/api/v2/summary.json | sha256sum` for tamper-evident snapshotting), action_tag_and_sha (e.g. `verify-wasm-pin-check@v1.17.0` SHA-pinned), ssh_trust_root_assertion (V1.17 Welle B SSH tag-signing remains independent of Sigstore — this field affirms the assertion is still verified), skipped_package_versions (the explicit list of `@atlas-trust/verify-wasm` versions whose Sigstore round-trip was skipped during the incident — closure §10.6 step 4 targets these for re-verification), gha_annotation_note (every CI run during the incident must emit a GHA annotation), operator_signoff.
- **§10.5 — 5-row escalation table.** Severity rows from "single Rekor 5xx burst" (resolve via re-run) → "Statuspage major incident <24h" (deferred-merge OK) → "Statuspage major incident ≥24h" (Trigger C activation) → "unscheduled `SIGSTORE_REKOR_V1_PEM` pubkey rotation announced" (verifier-core update required, NOT just a runbook activation) → **"Sigstore + `.github/allowed_signers` mutation observed simultaneously"** (designated **hard-pause** row — do NOT invoke §6, do NOT continue any release lane, do NOT commit the runbook update; treat as a coordinated cross-trust-root attack and escalate to off-channel maintainer comms). The hard-pause row is the design's anti-goal: §6 fallback assumes the SSH-tag trust root is uncompromised; if both Sigstore and allowed_signers move together, that assumption is invalidated.
- **§10.6 — 4-step closure.** Steps: (1) confirm Sigstore Public Good fully restored (Statuspage `operational` for ≥24h sustained); (2) re-verify all archived attestations (`npm audit signatures` on cached tarballs); (3) **target skipped versions** for re-verification — every version logged in §10.4 skipped_package_versions field gets a fresh round-trip; (4) close the incident_id in the audit log + cross-link to OPERATOR-RUNBOOK §15 if the incident triggered a pubkey rotation update. **In-place closure condition for OPERATOR-RUNBOOK §15 TBD case** documented: if §15 does not yet exist when closure is needed, the closure step references "OPERATOR-RUNBOOK §15 (TBD)" and the V1.18 Welle B (2) candidate `docs(v1.18/welle-b)` OPERATOR-RUNBOOK §15 inline-pin-update protocol becomes the natural follow-on to ship.
- **§10.7 — Six explicit non-goals.** (a) §10 is NOT a permanent-disable of Sigstore (incident-scoped only); (b) §10 does NOT change the publish lane (V1.17 Welle B SSH tag-signing remains the publish-side trust root regardless of consumer-side incident state); (c) §10 does NOT cover Atlas-internal Rekor degradation — that's a different incident class with different remediation; (d) §10 does NOT replace §6 reproduce-from-source — §6 is the offline fallback, §10 is the online-degraded protocol; (e) §10 does NOT modify any code or trust-root file — pure-docs by design; (f) §10 does NOT require a verifier-core update unless §10.5 row 4 (pubkey rotation) fires.

**Parallel-review correction record (per Atlas standing protocol — applied in-doc before commit):**

| Severity | Source | Issue | Fix location |
|----------|--------|-------|--------------|
| CRITICAL-1 | security-reviewer | Original §10.2 verification path used a single status-page check — defeatable by `sigstore.dev` DNS hijack (probe and Sigstore audit share fate). | §10.2 rewritten to three-source triangulation (Statuspage JSON + GitHub security advisories + ≥2 independent out-of-band maintainer signals); explicit fate-not-shared rationale added. |
| CRITICAL-2 | security-reviewer | Original §10.3 lockfile-bump row would have allowed install during incident with no integrity prerequisite — a trivial supply-chain injection window. | §10.3 lockfile-bump row: `npm ci --ignore-scripts` re-integrity prerequisite added; crates.io pre-cache mitigation documented; explicit "DO NOT install fresh during incident" guard. |
| CRITICAL-3 | code-reviewer | Original §10.4 audit-log spec did not include a tamper-evident status_snapshot field — meant a post-hoc rewrite of "what the status page said during the incident" was undetectable. | §10.4 status_snapshot field requires `curl ... \| sha256sum` capture-and-hash; verifier can confirm the operator's claim about Statuspage state at incident-time. |
| CRITICAL-4 | security-reviewer | Original §10.5 had no row for "Sigstore + allowed_signers mutate together"; the table read as a strict severity ladder where higher severity = more aggressive recovery. | §10.5 added the designated hard-pause row (the design's anti-goal); explicit "DO NOT invoke §6" + "treat as coordinated cross-trust-root attack" guidance; off-channel maintainer comms required. |
| CRITICAL-5 | code-reviewer | Original §10.6 closure had no targeted re-verification step for the versions skipped during the incident — meant skipped versions remained unverified indefinitely after incident close. | §10.6 step 3 "target skipped versions" added; cross-references §10.4 skipped_package_versions field; closure cannot complete without re-verification of every logged skipped version. |
| HIGH-1 | code-reviewer | §10.2 originally cited the wrong Atlassian Statuspage host. | Corrected to `https://www.sigstoreusergroup.io/api/v2/summary.json` + GitHub security-advisory API endpoint. |
| HIGH-2 | security-reviewer | §10.3 fresh-clone row originally suggested it was always safe to defer — failed to acknowledge that a downstream-consumer with a fresh clone has no working baseline and is the most degraded class. | §10.3 fresh-clone row: "most degraded class" caveat added; §6 reproduce-from-source explicitly cross-referenced as the only fully-verifiable path during incident. |
| HIGH-3 | code-reviewer | §10.4 did not specify that `verify-wasm-pin-check@v1` action_tag must be SHA-pinned in the audit log. | §10.4 action_tag_and_sha field requires both the human-readable tag (`@v1.17.0`) AND the SHA pin — re-tag-after-incident attack defended against. |
| HIGH-4 | security-reviewer | §10.5 row 4 (pubkey rotation) originally read "rerun verifier" — but a rotated `SIGSTORE_REKOR_V1_PEM` requires a verifier-core source update, not a runbook activation. | §10.5 row 4 corrected: "verifier-core update required, NOT just a runbook activation"; cross-references ADR-006 §5.2 (OPERATOR-RUNBOOK §15 TBD). |
| HIGH-5 | code-reviewer | §10.6 closure step 4 referenced OPERATOR-RUNBOOK §15 unconditionally — but §15 does not yet exist (Welle B (2) candidate). | §10.6 step 4 documented the in-place closure condition: if §15 does not exist at closure-time, reference as "TBD" and §10 itself becomes the trigger for the §15 work. |
| HIGH-6 | security-reviewer | §10.7 non-goals did not include the publish-lane-isolation guarantee — left ambiguous whether Sigstore incident affects publish-side trust. | §10.7 non-goal (b) added: "§10 does NOT change the publish lane — V1.17 Welle B SSH tag-signing remains the publish-side trust root regardless of consumer-side incident state". |
| HIGH-7 | code-reviewer | §10.4 ssh_trust_root_assertion field underspecified — did not say what the assertion's actual content is. | §10.4 ssh_trust_root_assertion: "affirms V1.17 Welle B SSH tag-signing trust root remains independent of Sigstore and is still verified at incident-time". |
| HIGH-8 | security-reviewer | §10.1 original framing implied §10 was a generic incident protocol — risked misreading as covering Atlas-internal Rekor or other unrelated outages. | §10.1 narrowed to "Sigstore Public Good infrastructure operated by the Sigstore Foundation" (excludes Atlas-internal infra); §10.7 non-goal (c) reinforces. |

**MED noted as follow-on (not blocking the runbook ship):** OPERATOR-RUNBOOK §15 inline-pin-update protocol (ADR-006 §5.2) does not yet exist; §10.6 closure step 4 references it as TBD. This is correct: shipping §10 first establishes the consumer-side incident protocol; §15 ships when the V1.18 Welle B (2) candidate is taken up. The forward-reference closure pattern is documented in §10.6 itself.

**CI gate behaviour on PR #7:** the V1.17 Welle C `verify-trust-root-mutations` workflow ran on the PR (it triggers on every PR, regardless of files changed) and PASSED in 5 seconds — re-confirming pure-docs PRs flow through the gate as a no-op. Same touchstone as PR #5: `docs/CONSUMER-RUNBOOK.md` is documentation, not in `PROTECTED_SURFACE`, not under `PROTECTED_PREFIXES` (`.github/actions/verify-wasm-pin-check/`).

**File shipped:** `docs/CONSUMER-RUNBOOK.md` (modified, +343 lines). New §10 inserted between §9 close and existing "## Reporting issues" tail. No other files touched.

---

## 1. V1.18 Welle A (2026-05-06) — Multi-Issuer Sigstore Tracking ADR (current)

**State summary:** the first Architecture Decision Record under `docs/ADR/` is shipped. ADR-Atlas-006 formally documents Atlas's three structurally distinct Sigstore touchpoints, the upstream multi-issuer landscape, and the explicit decision to **track (not adopt)** until concrete adoption triggers fire. Status field: `Tracking (not adopted)`. Date: 2026-05-06. Wave: V1.18 Welle A. PR: #5 (squash-merged as `c6aed22`).

**Why one ADR, not three (§1.2 of ADR):** all three touchpoints terminate at the same Rekor v1 production instance run by the Sigstore Foundation. A single Rekor incident degrades all three simultaneously; the decision-space (track, prepare, adopt, fall back) is identical for all three; splitting would force three near-duplicate decision records that drift over time.

**Three touchpoints documented (§1.1):**

- **Touchpoint A — Rekor v1 anchor verification (Atlas trust-core).** `crates/atlas-trust-core/src/anchor.rs` pins `SIGSTORE_REKOR_V1_PEM` (P-256 SPKI pubkey at line 83) and a 3-element `SIGSTORE_REKOR_V1_TREE_IDS` roster (line 138: active production shard `1_193_050_959_916_656_506` + two historical shards `3_904_496_407_287_907_110` and `2_605_736_670_972_794_746`). `SIGSTORE_REKOR_V1_ACTIVE_TREE_ID` (line 98) is the *issuer-side* anchor (atlas-signer always posts to the active shard); the *verifier-side* acceptance check `is_known_sigstore_rekor_v1_tree_id` does roster membership against the full slice so historical anchors captured under earlier shards still verify. Atlas accepts an anchor as Sigstore-verified if (a) its tree_id is a roster member, (b) the C2SP-format origin line `"rekor.sigstore.dev - {tree_id}\n"` reconstructs against that tree-ID, and (c) the anchor signature verifies against the pinned pubkey. Adding a new shard is intentionally a source change requiring a crate-version bump — silent acceptance of unknown tree-IDs is exactly the forgery primitive the roster forbids.
- **Touchpoint B — npm publish `--provenance` (`wasm-publish.yml`).** OIDC token from GitHub Actions ID-token service → Fulcio short-lived signing certificate → tarball SHA512 + workflow metadata signed → Rekor-attestation written. Single-issuer dependency: Fulcio + Rekor v1.
- **Touchpoint C — `npm audit signatures` round-trip (Welle A consumer-side).** `verify-wasm-pin-check@v1` Layer 3 self-test verifies the OIDC-Sigstore chain back to a configured trust root. Rekor outage during a consumer's CI run downgrades Layer 3 from active to passive — `docs/SECURITY-NOTES.md` **scope-k** covers this as a known degradation, including the air-gapped/offline-mirror note that explicitly bounds Sigstore network dependencies.

**Adoption triggers (§4):**

- **Trigger A** — npm trust root gains a second Fulcio CA OR a second Rekor log entry. Verification procedure: inspect npm bundled `trusted_root.json` at `$(npm root -g)/npm/node_modules/sigstore/store/trusted_root.json` + cross-reference upstream `sigstore/root-signing` + foundation announcement. **Known reproducibility weakness** acknowledged in-ADR: until npm exposes the trust root as first-class CLI output, the trigger is observable but not single-command-checkable.
- **Trigger B** — Rekor v2 issued for `@atlas-trust/verify-wasm`. Adoption work: extend `anchor.rs` for v2 entries via `SIGSTORE_REKOR_V2_PEM` + `SIGSTORE_REKOR_V2_TREE_ID` pair, update `crates/atlas-signer/src/rekor_client.rs` to negotiate v2 endpoints, port golden-test fixtures, validate cross-format mixed-mode (v1+v2 anchors in same chain). Estimate ~1 week.
- **Trigger C** — Sigstore Public Good extended incident (≥24h availability/integrity loss, or unscheduled `SIGSTORE_REKOR_V1_PEM` rotation, or documented integrity breach). Atlas response: **operator-runbook activation** (switch consumer guidance to Welle B SSH-tag-only verification), with adoption planning starting in parallel. **Documentation gap acknowledged in-ADR**: CONSUMER-RUNBOOK has no §10 "Sigstore Public Good incident — extended degradation protocol" section. Filling this gap is an **explicit prerequisite for Trigger C readiness** — recorded as a V1.18 Welle B follow-on task (`docs(consumer-runbook)/sigstore-incident-protocol`) to ship before the next quarterly ADR refresh.
- **Trigger D** — Atlas-side compliance requirement (customer audit, regulatory framework, SOC-2 line item) explicitly demands multi-issuer redundancy. Differs from A/B/C in that response is requirement-driven, not upstream-readiness-driven; may force a partial / alpha-quality multi-issuer verifier ship to meet a contractual deadline.

**Triggers that explicitly do NOT fire adoption (§4):** foundation announcements without deployed second issuer, non-npm trust-root additions, internal-corporate-only deployments, academic schemes without registry adoption. Principle: track production-deployable redundancy, not architectural readiness.

**Mitigations recognised (§6) — why "tracking, not adopting" is acceptable today:**

- Welle B SSH-tag primary trust root + Welle C trust-root mutation defence cover the ship-side trust root independently of Sigstore state.
- Compile-time pinned Rekor v1 anchor in `anchor.rs` (false-negative-safe under key rotation; operator response is to ship a verifier-core update with the new pin, golden-test verified).
- TOCTOU-pinned GH-Release via `--target ${VERIFIED_SHA}` (V1.17 Welle B): Sigstore-side compromise of npm provenance cannot retroactively rewrite the GH-Release-distributed tarball binding.
- Per-algo minimum base64 payload length checks in `verify-wasm-pin-check@v1` (V1.17 Welle A post-review): empty-hash bypass blocked even if a Sigstore-level signature were somehow valid for an empty payload.
- A Sigstore Public Good incident degrades **one** of three Atlas verification surfaces; **two** of three (Welle B+C, anchor-pinning) remain fully active.

**Atlas-side preparation (§5) — three follow-ons recorded:**

- **§5.1** — refactor `anchor.rs` to a `RekorIssuer { name, pem, active_tree_id, tree_id_roster }` registry pattern + `&[&RekorIssuer]` slice. Mechanical, pure-internal-API, no semantics change. Lowers future Rekor-v2-adoption cost. **Status:** deferred to V1.18 Welle B+ as a candidate.
- **§5.2** — document the inline-pin-update protocol in `OPERATOR-RUNBOOK.md` §15 (TBD): PR review requirements, golden-test fixture regeneration, cross-version-anchor compatibility test. Currently implicit. **Status:** deferred to V1.18 Welle B as a `docs(v1.18/welle-b)` task.
- **§5.3** — `SIGSTORE_DEGRADED_OK` flag for `verify-wasm-pin-check@v1`. **OPEN PROBLEM (NOT solved by an obvious quick fix).** The naive defence — "require a fresh liveness probe to `https://rekor.sigstore.dev/api/v1/log/publicKey` before accepting degraded mode, with a per-repo per-week cap" — is **defeatable by the same network-positioned adversary class the feature is meant to exclude**. A targeted DNS or BGP hijack against `rekor.sigstore.dev` for the duration of one probe causes the probe to return 200 (because the probe and the audit round-trip both resolve to the same domain via the same path) while the attacker simultaneously serves a forged "no attestation" response or a stale-but-syntactically-valid attestation. The probe and the protected operation share a fate. The per-week cap mitigates opportunistic abuse but not a targeted attacker. Adequate independence requires probing through a route that does NOT share a fate with the audit round-trip — three candidates sketched: (i) cross-log consistency check against an independent Sigstore-compatible log (only possible after multi-issuer adoption — circular), (ii) Sigstore Foundation status page over a separately-anchored TLS certificate chain (different CA, different DNS authoritative servers), (iii) Atlas-operated witness recording canonical Rekor checkpoints out-of-band, itself anchored. None of (i)/(ii)/(iii) is trivially correct. **Decision:** record intent + naive defence + why-it-fails + defer the actual design to a future ADR-Atlas-00X addressing fate-shared-channel verification specifically. **Do NOT ship `SIGSTORE_DEGRADED_OK` until that ADR lands.**

**Watchlist + review cadence (§7):** Sigstore project blog, Rekor releases on GitHub, sigstore/protobuf-specs, npm CLI changelog, tuf-conformance test additions, Sigstore Public Good ops status page. Quarterly refresh of §3 status numbers; incident-triggered refresh within one operational week of a Trigger C event; major-Atlas-Vx.0 cut full re-evaluation.

**Parallel-review correction record (per Atlas standing protocol — applied in-doc before commit):**

| Severity | Source | Issue | Fix location |
|----------|--------|-------|--------------|
| CRITICAL-1 | security-reviewer | §1.1 Touchpoint A originally framed as single-pin verification; actual code is roster membership against `SIGSTORE_REKOR_V1_TREE_IDS`. | §1.1 rewritten to correctly describe issuer-side anchor vs verifier-side roster-membership distinction. |
| CRITICAL-2 | code-reviewer | Wrong constant name `SIGSTORE_REKOR_V1_INACTIVE_TREE_IDS` (does not exist); actual is `SIGSTORE_REKOR_V1_TREE_IDS` (single roster, active + historical). | §3.1 + §5.1 corrected. |
| CRITICAL-3 | both reviewers | scope-l vs scope-k cross-reference error. scope-k = Welle A (consumer action); scope-l = Welle B (tag-signing). | §1.1 Touchpoint C + §6 corrected. |
| HIGH-1 | code-reviewer | §4 Trigger A "verify via `npm audit signatures --json`" not testable — that command does NOT expose the bundled TUF root. | §4 Trigger A: added concrete three-source verification procedure (npm bundled `trusted_root.json`, upstream `sigstore/root-signing` cross-reference, foundation announcement) + reproducibility-weakness acknowledgment. |
| HIGH-2 | security-reviewer | §5.3 naive liveness-probe defence defeatable by network-positioned attacker (probe and audit share fate). | §5.3 rewritten: "naive defence and why it fails" + three independent-channel candidates + actual design deferred to a future ADR. |
| HIGH-3 | security-reviewer | §4 Trigger C documentation gap — CONSUMER-RUNBOOK has no Sigstore-incident protocol. | §4 Trigger C: gap acknowledged in-doc; follow-on task recorded as a V1.18 Welle B task. |

**MED noted as follow-on (not blocking the ADR ship):** forward link from `SECURITY-NOTES` scope-k to ADR-006 not yet added — tracked as a separate `docs(security-notes)/scope-k-forward-link-adr-006` task. The ADR's existing reference to scope-k is correct and sufficient for the ship direction.

**CI gate behaviour on PR #5:** the V1.17 Welle C `verify-trust-root-mutations` workflow ran on the PR (it triggers on every PR, regardless of files changed) and PASSED in 5 seconds — confirming the ADR is correctly *outside* `PROTECTED_SURFACE` and `PROTECTED_PREFIXES`. Pure-docs changes flow through Welle C cleanly. Touchstone for future pure-doc PRs: PROTECTED_SURFACE is the ten files listed in `tools/verify-trust-root-mutations.sh`; PROTECTED_PREFIXES is the `.github/actions/verify-wasm-pin-check/` subtree; everything else (including `docs/ADR/`, `docs/CONSUMER-RUNBOOK.md`, this handoff file) is unprotected and the gate is a 5-second no-op.

**File shipped:** `docs/ADR/ADR-Atlas-006-multi-issuer-sigstore-tracking.md` (228 lines, sections §1 Context through §9 Decision Log). First file under `docs/ADR/` — establishes the directory and the format. Future ADRs follow the numbered `ADR-Atlas-NNN-slug.md` naming and the same nine-section template.

---

## V1.17 sign-off

V1.17 Welle A + Welle B + Welle C all SHIPPED on 2026-05-05. Tag `v1.17.0` LIVE at commit `d955567` (post-CI-fix). All three anti-drift gates green: Welle A `PASS: 40 / 40`, Welle B `PASS: 14 / 14`, Welle C `PASS: 17 / 17`. Operator-side ceremony (Repository Ruleset `Master trust-root protection` with `bypass_actors: []`) bound to `master` branch protection. **Full V1.17 history preserved at `.handoff/v1.17-handoff.md`** (audit-only — read for design-decision provenance on composite-bash-vs-TypeScript-action, ASCII Unit Separator field-passing, npm-ls precondition, SSH-vs-Sigstore-vs-GPG tag-signing, in-repo-allowed_signers vs out-of-band trust root, `pull_request` vs `pull_request_target` trigger choice, PROTECTED_SURFACE exact-match vs PROTECTED_PREFIXES subtree-match, anti-rewrite bootstrap defence, and the four-pass parallel-review fix triage).

V1.16 + V1.15 + earlier: see the corresponding `.handoff/v*.md` files. Each major V is its own handoff file; the active one is the latest (`v1.18` currently).

---

## Standing protocol (re-read each session)

1. **Implement → parallel review → fix → single coherent commit.** For any non-trivial change, run `code-reviewer` and `security-reviewer` agents in parallel against the staged diff. Fix all CRITICAL and HIGH findings in-commit (do not split). Selectively fix MEDIUMs that don't expand scope. Document the fix triage in the commit message body. Single `feat(v1.X/welle-Y)` or `docs(v1.X/welle-Y)` commit, then a separate `docs(v1.X/handoff)` commit prepending the new status block above §0 in this file.

2. **PR-then-merge, not direct push.** Any change to `master` goes through a PR. The Welle C `verify-trust-root-mutations` gate runs on every PR; trust-root-surface changes require a commit signed by a key in the *prior* `.github/allowed_signers`. Pure-docs PRs (this file, ADRs, runbooks outside the protected list) flow through cleanly.

3. **Anti-drift gates before any V1.18+ edit.** Run all four (Welle A 40/40, Welle B 14/14, Welle C 17/17, playground CSP) and confirm green BEFORE touching anything. If any gate is red on a clean tree, the harness itself has regressed — stop and investigate before adding new work.

4. **German in chat, English in code/comments/commits.** User preference is German in conversation; project artefacts (commit messages, PR bodies, ADRs, code comments) stay English for downstream-consumer compatibility.

5. **Standing directive:** *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Entscheide du, was jetzt das Bestmögliche ist anhand deiner Informationen, die du hast. Immer beste Sicherheit und beste Codequalität."* — autonomous design choices when explicit, security-first, codequality-first; report the decision tree in the commit message rather than asking for explicit approval on each design dimension.

6. **Confirm scope in one sentence before starting non-trivial work.** "Ich öffne V1.18 Welle B (1) — `docs(consumer-runbook)/sigstore-incident-protocol`. OK?" — then proceed.
