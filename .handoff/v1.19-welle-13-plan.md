# V1.19 Welle 13 — Plan-Doc (v1.0.0 Ship)

> **Status: DRAFT 2026-05-11.** Awaiting Nelson's confirmation. Becomes the SHIPPED block in `.handoff/v1.19-handoff.md` §0 once merged + tagged.

Welle 13 ist der **v1.0.0 ship**. Mechanically straightforward — version bumps + CHANGELOG finalisation + signed tag + GitHub Release + npm publish auto-fires. The intellectual work was done in Welle 12 (SemVer audit). Welle 13 ist execution.

## Scope

| In-Scope | Out-of-Scope |
|---|---|
| Cargo workspace version bump 0.1.0 → 1.0.0 (Cargo.toml line 23) | playground.atlas-trust.dev verifier-version chip update (separate post-tag PR) |
| npm version bumps for atlas-web, atlas-mcp-server, @atlas/bridge, root | Demo-Ready landing page + Quickstart Guide (Welle 14) |
| MCP server SDK introspection string bump (`apps/atlas-mcp-server/src/index.ts:35`) | Sub-path export gating for `__*ForTest` family (post-1.0 minor, audit-driven) |
| Doc-comment example bump (`crates/atlas-trust-core/src/verify.rs:137`) | V2 forward-break candidates (e.g. `--require-strict-chain` default-flip — SemVer-major, not v1.x) |
| Cargo.lock + pnpm-lock.yaml regen | New features or API surfaces |
| CHANGELOG.md `[Unreleased]` → `[1.0.0]` finalisation | Cargo publish to crates.io (npm publish is in scope; crates.io publish is V2 candidate) |
| `## Security Advisories` section in CHANGELOG (Welle 12 F-6 carry-forward, CVE-prep metadata for Welle 9 SR-H findings) | Performance benchmarks |
| Signed tag `v1.0.0` via V1.17 SSH-Ed25519 path | Example integrations under `examples/integrations/` (Welle 14) |
| GitHub Release with release notes from CHANGELOG | |
| `wasm-publish.yml` auto-fires on tag-push → `@atlas-trust/verify-wasm@1.0.0` to npm with Sigstore provenance | |

## Decisions (final, per Nelson 2026-05-11)

- **CHANGELOG Security Advisories section:** YES — standard CVE-prep section with per-finding affected-version-range, CVSS-adjacent severity-token, first-safe-version. Documents Welle 9 SR-H-1 (empty-trace silent pass) + SR-H-2 (self-reference 1-cycle bypass).
- **Playground update timing:** separate post-tag PR. Welle 13 stays version-bump-only.
- **Sigstore Rekor anchor:** rely on `wasm-publish.yml` auto-fire — `npm publish --provenance` produces a Sigstore Rekor attestation as SLSA L3 provenance. That's the public-trust anchor for the v1.0 release. Git-tag-commit anchor as separate cosign-style step deferred to post-1.0 if needed (npm provenance is the auditor-facing artefact).
- **Wasm-package version sync:** `crates/atlas-verify-wasm/Cargo.toml` uses `version.workspace = true`, so the Cargo bump auto-propagates. The `@atlas-trust/verify-wasm` npm package version is set by `wasm-pack` from the Cargo manifest at publish time. No manual file edit needed for the wasm npm version.
- **Build artefacts left alone:** `apps/wasm-playground/pkg/package.json`, `crates/atlas-verify-wasm/pkg/package.json`, `crates/atlas-verify-wasm/pkg-test/package.json` are `wasm-pack` outputs. Auto-regenerated. NOT committed in this PR — they regenerate on next build (CI or local).

## Files

| Status | Pfad | Inhalt |
|---|---|---|
| MODIFY | `Cargo.toml` | `workspace.package.version = "0.1.0"` → `"1.0.0"` (line 23). All 5 workspace crates inherit via `version.workspace = true`. |
| MODIFY | `package.json` (root) | `"version": "0.1.0"` → `"1.0.0"` (line 3) |
| MODIFY | `apps/atlas-web/package.json` | `"version": "0.1.0"` → `"1.0.0"` (line 3) |
| MODIFY | `apps/atlas-mcp-server/package.json` | `"version": "0.1.0"` → `"1.0.0"` (line 3) |
| MODIFY | `packages/atlas-bridge/package.json` | `"version": "0.1.0"` → `"1.0.0"` (line 3) |
| MODIFY | `apps/atlas-mcp-server/src/index.ts` | `version: "0.1.0"` → `"1.0.0"` (line 35, MCP SDK introspection string) |
| MODIFY | `crates/atlas-trust-core/src/verify.rs` | doc-comment example `"atlas-trust-core/0.1.0"` → `"atlas-trust-core/1.0.0"` (line 137, illustrative only) |
| MODIFY | `Cargo.lock` | Auto-regen via `cargo build --workspace` |
| MODIFY | `pnpm-lock.yaml` | Auto-regen via `pnpm install` |
| MODIFY | `CHANGELOG.md` | `[Unreleased]` → `[1.0.0]` promotion, new release-summary entry + new `## Security Advisories` section before `## V1.19 Welle 10` |
| NEW | `.handoff/v1.19-welle-13-plan.md` | This plan-doc itself |

## Test impact (which assertions might shift)

After Cargo version bump, `VERIFIER_VERSION` const in `crates/atlas-trust-core/src/lib.rs:47` becomes `atlas-trust-core/1.0.0` (derives from `CARGO_PKG_VERSION`). This appears in:

| Surface | Pinned-as | Drift risk |
|---|---|---|
| `VerifyOutcome.verifier_version` JSON field | atlas-trust-core/X.Y.Z | None — consumer regex on prefix |
| `print_human` "Verifier:" line | atlas-trust-core/X.Y.Z | None — Welle 12 audit §1.1 documents this as "Locked format `crate-name/semver`" |
| atlas-web LiveVerifierPanel chip text | atlas-trust-core/X.Y.Z | None — `home.spec.ts:50` regex pins `/^atlas-trust-core\//` (prefix only) |
| MCP smoke output | "Verifier: atlas-trust-core/X.Y.Z" | None — smoke doesn't pin the version literally |
| atlas-web e2e write roundtrip output | "Verifier: atlas-trust-core/X.Y.Z" | None — e2e doesn't pin the version literally |

**No test version-string assertion breaks.** This was the Welle 11 anti-drift convention being load-bearing.

## CHANGELOG Security Advisories section (concrete content)

NEW section between current `## [Unreleased]` body and `## V1.19 Welle 10` entry:

```markdown
## Security Advisories

The following findings were discovered and remediated during Atlas's
pre-1.0 development. Documented here for downstream CVE-prep workflows.

### ATLAS-2026-001 (V1.19 Welle 9 SR-H-1): Empty-trace strict-chain silent pass

- **Severity:** HIGH (CVSS-equivalent: integrity / auditor-trust)
- **Affected:** all pre-1.0 builds with `--require-strict-chain` flag enabled
  (the flag itself shipped in V1.19 Welle 9 / commit e650f93 / PR #42).
- **First safe version:** v1.0.0 (this release). The fix landed in-commit
  in the same PR that introduced the flag, so the public flag was never
  affected — flagged here for completeness of the audit trail.
- **Description:** An earlier draft of `check_strict_chain` used the shape
  `if events.len() != 1 ...` which would have silently passed an empty
  trace under strict mode. Strict mode pins five properties including
  "non-empty"; without this, an attacker who stripped events from a bundle
  could pass strict mode silently.
- **Remediation:** `check_strict_chain` now returns
  `TrustError::StrictChainViolation` with diagnostic "trace has no events
  (a linear chain requires at least 1 genesis event)" as the first check.

### ATLAS-2026-002 (V1.19 Welle 9 SR-H-2): Self-reference 1-cycle bypass

- **Severity:** HIGH (CVSS-equivalent: integrity / auditor-trust)
- **Affected:** all pre-1.0 builds with `--require-strict-chain` flag enabled
  (the flag itself shipped in V1.19 Welle 9 / commit e650f93 / PR #42).
- **First safe version:** v1.0.0 (this release). Fix in-commit; public flag
  was never affected.
- **Description:** A 1-event trace where the event lists its own
  `event_hash` as a parent (cryptographically infeasible after a
  successful `check_event_hashes` pass under blake3 preimage resistance,
  but a defence-in-depth concern when `check_strict_chain` is called
  standalone) would have failed with a misleading "found 0 genesis events"
  message instead of the structured "self-reference cycle" diagnostic.
- **Remediation:** Self-reference check positioned FIRST among per-event
  checks in `check_strict_chain`, so a 1-event self-ref reports the cycle
  diagnostic correctly before the genesis-count check fires.

Both findings were closed in-commit during the V1.19 Welle 9 review pass
(2026-05-09); v1.0.0 is the first version with the documented audit trail.
```

## Implementation phases

1. **Plan-doc approval** (this step) — Nelson confirms scope.
2. **Cargo workspace bump:** edit `Cargo.toml` line 23, run `cargo build --workspace --features hsm` to regen `Cargo.lock`.
3. **npm bumps:** edit 4 `package.json` files, run `pnpm install --frozen-lockfile=false` to regen `pnpm-lock.yaml`. Lock file should only change the four version strings; if it changes more, investigate.
4. **MCP SDK string bump:** edit `apps/atlas-mcp-server/src/index.ts:35`.
5. **Doc-comment example bump:** edit `crates/atlas-trust-core/src/verify.rs:137`.
6. **CHANGELOG finalisation:**
   - Move `[Unreleased]` heading + body to a new `## [1.0.0] — 2026-05-11` section.
   - Add a "v1.0.0 Release Summary" sub-section under `[1.0.0]` that distills the V1.0-baseline → V1.19 Welle 12 trust-property progression (one paragraph).
   - Add new `## Security Advisories` section (content above).
   - Update `[Unreleased]` to empty placeholder.
   - Update version-link footer.
7. **Local validation:** `cargo test --workspace --features hsm`, `pnpm install`, all build commands, all e2e/Playwright/smoke contracts intact. The `VERIFIER_VERSION` const now reads `atlas-trust-core/1.0.0` — verify the smoke + e2e outputs show this.
8. **Single SSH-signed feat commit:** branch `welle/v1.19-welle-13-v1.0.0-ship`, conventional-commits `feat(v1.19/welle-13): v1.0.0 — version bumps + CHANGELOG finalisation + Security Advisories`.
9. **PR + wait-for-CI + standard REST squash-merge (no `--admin`).**
10. **Parallel review:** code-reviewer + security-reviewer on the diff. Expected scope: minimal (version bump diff is tiny + CHANGELOG narrative). Security-reviewer focus: Security Advisories wording correctness (no over-claiming, no under-claiming, no exposure of new info beyond what's already public in handoffs + commit messages).
11. **Fix any CRITICAL/HIGH/actionable MEDIUMs in-commit** (expected: probably 0).
12. **Tag v1.0.0** via SSH-Ed25519 on the merge commit: `git tag -s -m "v1.0.0 — Atlas v1.0 release" v1.0.0 <merge-sha>` (or `-S` for the SSH-sign equivalent per V1.17 path).
13. **Push tag:** `git push origin v1.0.0`. This auto-fires `wasm-publish.yml`:
    - `verify-tag-signatures` step gates (V1.17 Welle B contract).
    - Build wasm via `wasm-pack` for browser + node targets.
    - `npm publish --provenance` for `@atlas-trust/verify-wasm@1.0.0` (provenance generates Sigstore Rekor attestation as SLSA L3 — the public-trust anchor).
    - `gh release create v1.0.0 --target ${VERIFIED_SHA}` (TOCTOU-pinning per V1.17).
14. **GitHub Release notes:** the wasm-publish workflow creates the Release skeleton; we then update with release notes distilled from the `[1.0.0]` CHANGELOG entry (manual `gh release edit v1.0.0 --notes-file ...`).
15. **Docs PR:** Welle 13 SHIPPED block, §-renumber, new Welle-14-candidates section retitle.
16. **Vault sync:** Atlas.md status `v1.0.0-shipped`, Journey-13 final row, new note `Atlas/28 - V1.19-Welle-13-V1.0.0-Ship.md`.

## Anti-Drift Contract (Welle-13)

- **No new public APIs.** Welle 13 is purely version-string changes + CHANGELOG narrative. Any code surface change would invalidate Welle 12's SemVer audit baseline.
- **CHANGELOG `[Unreleased]` → `[1.0.0]` promotion is one-way.** After Welle 13 merges, future PRs add new entries under `[Unreleased]` again.
- **Tag `v1.0.0` SSH-signed via the V1.17 path.** No `--no-gpg-sign`. No skipping the verify-tag CI gate. Tag MUST appear in `git verify-commit`/`git verify-tag` as `Good "git" signature for nelson@ultranova.io with ED25519 key SHA256:qq/VVJYpsgEdeQSLqU0QS/gKn6ohXJHio+VkzVX+4Zg`.
- **No `--admin` override on the PR merge.** Standard REST squash-merge per V1.18 Welle B (3) merge-discipline.
- **`wasm-publish.yml` auto-fire is load-bearing.** Do NOT manually run `npm publish` from a workstation — that would bypass the verify-tag-signatures gate + skip the SLSA provenance attestation.

## Validation evidence required

- `cargo test --workspace --features hsm` — all green (incl. unchanged 9 hashchain strict-chain tests + 6 strict_mode CLI tests + 130 trust-core lib tests)
- `cargo test -p atlas-verify-cli --test strict_mode` — 6/6
- `pnpm install --frozen-lockfile=false` then `pnpm install --frozen-lockfile` — exits 0 (lockfile in sync)
- `pnpm --filter @atlas/bridge build` — exit 0
- `pnpm --filter atlas-mcp-server build` — exit 0
- `pnpm --filter atlas-mcp-server test` — 90/90
- `pnpm --filter atlas-mcp-server smoke` — ✓ VALID both legs; **the new `Verifier: atlas-trust-core/1.0.0` line in stdout is the most visible Welle-13 ship signal**
- `pnpm --filter atlas-web build` — exit 0
- `pnpm --filter atlas-web e2e:write` — ✓ VALID with `atlas-trust-core/1.0.0` Verifier line
- `pnpm --filter atlas-web e2e:edge-cases` — 42/42
- `pnpm --filter atlas-web exec playwright test` — 38/38 (Welle 11 contract intact; the `verifier-version` chip now shows `atlas-trust-core/1.0.0`, but the regex pin is prefix-only)
- All Welle 1-12 anti-drift gates remain green
- `git tag -v v1.0.0` (post-tag) → `Good "git" signature ...`
- npm registry `npm view @atlas-trust/verify-wasm@1.0.0` (post-publish) → shows version 1.0.0 with provenance attestation
- GitHub Release `v1.0.0` published with notes

## Risks + Mitigations

| Risk | Mitigation |
|---|---|
| Lockfile regen pulls in unrelated dependency updates | Run `pnpm install --frozen-lockfile=false` and diff `pnpm-lock.yaml` carefully — if non-version-related changes appear, investigate before committing. Use `--lockfile-only` if available. |
| `cargo build --workspace --features hsm` fails on Windows due to HSM crate native deps | Run from MSYS bash with the same env as Welle 10/11/12. If HSM build breaks, fall back to `cargo build --workspace` (without `--features hsm`) for Cargo.lock regen; the workspace lock doesn't change between feature sets. |
| Tag push triggers wasm-publish.yml but `NPM_TOKEN` not configured → publish skipped silently | Pre-check secrets are set in repo settings. If publish is skipped, the GH-Release still produces, npm package can be published manually post-hoc (low risk, not load-bearing for v1.0 release artefact list). |
| Wasm-publish workflow has additional required gates we haven't anticipated (e.g. SLSA build-checksum mismatch) | Read the workflow YAML thoroughly before tag-push. Walk the verify-tag → build → publish chain. |
| Security Advisories section over-claims severity (e.g. SR-H-1 was never in a public release with the flag enabled) | Worded to clarify "fix in-commit in same PR that introduced the flag, so public flag was never affected — flagged here for completeness of audit trail". |
| Security Advisories section under-claims (an attacker who already has stripping-events capability could have used SR-H-1 to silently pass strict mode pre-fix) | Description includes "an attacker who stripped events from a bundle could pass strict mode silently" — accurate threat-model framing. |
| Welle 12 docs PR's paths-filter fix (`.handoff/**` → atlas-web-playwright) means Welle 13 docs PR will also trigger the full Playwright run (~7 min CI) | Acceptable. Welle 12 fixed the gate-not-reachable bug; the ~7 min cost is amortised across rare doc PRs. |
| Tag conflicts: a `v1.0.0` tag might already exist locally or remotely (e.g. abandoned earlier attempt) | `git tag -l v1.0.0` + `git ls-remote --tags origin v1.0.0` to check before creating. If exists locally only, delete with `git tag -d v1.0.0`. If exists remotely, abort and escalate to Nelson. |
| GitHub Release notes formatting drift between `.handoff/v1.19-handoff.md` Welle 13 SHIPPED block and CHANGELOG `[1.0.0]` section | Single-source: CHANGELOG `[1.0.0]` is the canonical release notes; handoff §0 cross-references it. |

## Acceptance criteria

- [ ] All 8 files modified per the Files table
- [ ] `Cargo.lock` and `pnpm-lock.yaml` regenerated, no unrelated changes
- [ ] All Welle 1-12 anti-drift gates remain green
- [ ] `cargo test --workspace --features hsm` exits 0, smoke + e2e show `atlas-trust-core/1.0.0` in Verifier line
- [ ] CI green at PR-merge time
- [ ] Single SSH-signed feat commit merged via standard REST squash-merge
- [ ] Tag `v1.0.0` SSH-signed and pushed to origin
- [ ] `wasm-publish.yml` triggered by tag-push, completes green
- [ ] `@atlas-trust/verify-wasm@1.0.0` visible on npm with provenance attestation
- [ ] GitHub Release `v1.0.0` published with release notes from CHANGELOG `[1.0.0]` section
- [ ] Welle 13 SHIPPED block in `.handoff/v1.19-handoff.md` + new vault note `Atlas/28` + Atlas.md status update

## Post-Welle-13 follow-ups (Welle 14+ candidates)

- **playground.atlas-trust.dev verifier-version chip update** — separate post-tag PR. Wait for `@atlas-trust/verify-wasm@1.0.0` to land on npm, then bump the worker dependency.
- **Sigstore Rekor anchor of v1.0.0 tag commit** (separate from npm-package provenance) — defer to post-1.0 if needed; the npm-publish-provenance is the auditor-facing public-trust artefact.
- **Welle 14 Demo-Ready Surfaces** — landing page, Quickstart, example integrations under `examples/integrations/`. Welle 11 + Welle 12 carry-forward visual polish folds in here.
- **Cargo publish to crates.io** — V2 candidate. Atlas currently ships only the wasm npm package; the Rust crates are workspace-internal.
