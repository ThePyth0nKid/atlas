# V1.19 — Agent Handoff (Welle 1 SHIPPED)

You are continuing the Atlas Verifiable Knowledge Graph project. **V1.19 Welle 1 — Atlas-Web Write-Surface — shipped on 2026-05-08 as squash-merged commit `3853c64` (PR #26).** This is the first user-facing write path: a Browser-User can now create an Atlas node via `POST /api/atlas/write-node` + `/write` page, with structurally identical trust-property to MCP-host writes (same Rust signer, same per-workspace HKDF-derived Ed25519 key, same append-only JSONL DAG — the Verifier cannot distinguish web-emitted from MCP-emitted events).

V1.18 (Welle A + Welle B 1–8) remains the current trust posture for downstream-consumable releases. See `.handoff/v1.18-handoff.md` for the V1.18 history; this handoff covers ONLY V1.19 Welle 1.

The user prefers **German** in chat; respond in German. Standing directive: *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Entscheide du, was jetzt das Bestmögliche ist anhand deiner Informationen, die du hast. Immer beste Sicherheit und beste Codequalität."*

> ⚠️ **Read this in full before touching code.** Captures the V1.19 Welle 1 ship state (commit `3853c64`, PR #26, two parallel-review iterations with 1 CRITICAL Mutex Map-Identity Race + 1 HIGH ARG_MAX-overflow + MEDIUM unbounded-runProcess + LOW body-size cap + CSRF + rate-limiting advisories all closed in-commit), the V1.19 Welle 2 candidate list (deferred LOW findings without trust impact), the rationale (why atlas-web write-surface was the right pivot after 18 waves of trust-hardening; why per-tenant kid auto-derivation is mandatory and the MCP-tool's caller-supplied-kid affordance is *deliberately not mirrored*; why "no auth" is a deliberate non-goal and the operator-boundary lives in OPERATOR-RUNBOOK §17), and the Obsidian-vault updates folded into this session.

> **If you only read three things:** (1) "Session opener — start here" right below — recommended next action; (2) §0 "V1.19 Welle 1 — SHIPPED 2026-05-08" — ship summary, validation evidence, what landed; (3) §4 "V1.19 Welle 2 candidates" — concrete next-wave options.

> **Doc structure:** §0 = SHIPPED block (this wave). §1 = V1.19 Welle 1 implementation context. §2 = Files landed in `3853c64`. §3 = Review-correction record. §4 = V1.19 Welle 2 candidates. §5 = Obsidian vault updates this session. §"Standing protocol" = workflow rules.

---

## Session opener — start here

**Working tree state (as of 2026-05-08, V1.19 Welle 1 SHIPPED):** on `master`, clean, in sync with `origin/master`. Top of `git log --oneline -3` is `3853c64 feat(v1.19/welle-1): atlas-web write surface (#26)` → `6c50699 docs(v1.18/handoff): V1.18 Welle B (8) SHIPPED (#25)` → `d650b5b feat(v1.18/welle-b-8): cross-version-anchor-compat pre-merge gate (#24)`. PR #26 squash-merged via standard REST `PUT /pulls/26/merge` (no `--admin` override per V1.18 Welle B (3) merge-discipline). Both CI checks green (`Verify trust-root-modifying commits` 8 s; `wave3-smoke` 3 m 19 s). SSH-signed commit (ed25519 `SHA256:qq/VVJYpsgEdeQSLqU0QS/gKn6ohXJHio+VkzVX+4Zg`).

**Verify-before-touch checklist** (run these to confirm baseline before any V1.19 Welle 2 edit):
- `git status` → clean. `git log --oneline -3` → `3853c64` → `6c50699` → `d650b5b`.
- `cd apps/atlas-web && pnpm tsc --noEmit` → exit 0.
- `cd apps/atlas-web && pnpm build` → exit 0 (Turbopack-trace warning on `next.config.ts` import is pre-existing and benign).
- `cd apps/atlas-web && pnpm e2e:write` → ✓ VALID, all 9 evidence checks green, strict per-tenant mode (`ATLAS_DEV_MASTER_SEED=1` is set inside the script).
- `bash tools/test-trust-root-mutations.sh` → exit 0, `PASS: 17 / 17`. Case 7 PROTECTED_SURFACE/CODEOWNERS parity reports **18 paths** unchanged from V1.18 Welle B (8) — V1.19 Welle 1 added zero PROTECTED_SURFACE entries (bridge layer is `apps/`-side, not part of the trust-root surface).
- `bash tools/test-tag-signatures.sh` → exit 0, `PASS: 13 / 13`.
- `bash .github/actions/verify-wasm-pin-check/test/run-tests.sh` → exit 0, `PASS: 40 / 40`.
- `bash tools/playground-csp-check.sh` → exit 0, `PASS`.
- `/c/Users/nelso/.cargo/bin/cargo.exe test --features hsm --workspace` → 347 passing.

**Recommended next action (default if user says "weiter" / "go" / "next"):**

→ **Open V1.19 Welle 2.** Welle 1 has shipped; the natural next step is the bridge-layer consolidation that Welle 1's review surfaced as the highest-leverage cleanup. Two coherent options, the user has not yet picked between them:

1. **`packages/atlas-bridge/` extraction (preferred).** Consolidate `apps/atlas-web/src/lib/atlas/` and `apps/atlas-mcp-server/src/lib/` into a single workspace package. The two trees were brought to mutex parity in `3853c64`, but `WriteEventResult` kid-field shapes have already diverged once and will drift again under any change touching both. Extracting now is cheaper than every future fix re-paying the parity tax. Touches no trust-root surface (anti-drift gates remain green by construction). Estimated 2–4 files moved + 1 new `package.json`; both apps re-import from `@atlas/bridge`.

2. **Carry-forward LOW findings without extraction.** Smaller, faster, no architectural change: tighten `redactPaths` regex (false-positives on non-paths), add `resolveSignerBinary` cache TTL + `existsSync` re-validation, refactor `ulid()` to pure-function form, verify e2e dynamic-import hoisting, expand Playwright UI-level E2E coverage. None block #1; all become free once the bridge is one package.

If the user is ambiguous, **prefer #1**: fixing in two places now is the standing-cost the standing-directive ("beste Codequalität") tells us to refuse.

**Workflow reminder:** Atlas standing protocol — implement → parallel `code-reviewer` + `security-reviewer` → fix CRITICAL/HIGH/selected MEDIUMs in-commit → SSH-signed feat commit + standard REST merge (no `--admin`) → docs handoff PR. Anti-drift gates as listed above. Cargo PATH on Windows non-default (use `/c/Users/nelso/.cargo/bin/cargo.exe`). Repo is Windows host with bash/MSYS — forward-slash paths in shell snippets. **`gh` CLI lives at `/c/Program Files/GitHub CLI/gh.exe`** (NOT in default PATH; use absolute path or `GH="/c/Program Files/GitHub CLI/gh.exe" ; "$GH" ...`).

**Before you touch anything**, confirm scope with the user in one sentence, then proceed under the standing directive.

---

## 0. V1.19 Welle 1 — SHIPPED 2026-05-08

**Ship summary:** PR [#26](https://github.com/ThePyth0nKid/atlas/pull/26) squash-merged into commit `3853c64` on `master` at 2026-05-08. 19 files, +1881 / −15 LoC. Both CI checks green. SSH-signed; merged via standard REST `PUT /pulls/26/merge` with `merge_method=squash` and **no `--admin` override** (V1.18 Welle B (3) merge-discipline upheld).

**Validation evidence at ship time:**

| Gate | Result |
|---|---|
| `pnpm tsc --noEmit` (atlas-web) | exit 0 |
| `pnpm build` (atlas-web, Next 16) | exit 0 |
| `pnpm e2e:write` (write-node round-trip) | ✓ VALID strict per-tenant, **9/9 evidence checks** green; both POSTs chained; path-traversal `workspace_id` rejected at 400; 2 events on disk match POST results |
| `tools/test-trust-root-mutations.sh` | 17/17 PASS, **PROTECTED_SURFACE 18 paths unchanged** from V1.18 Welle B (8) |
| `tools/test-tag-signatures.sh` | 13/13 PASS |
| `verify-wasm-pin-check/test/run-tests.sh` | 40/40 PASS |
| `tools/playground-csp-check.sh` | PASS |
| GitHub Actions `Verify trust-root-modifying commits` | PASS (8 s) |
| GitHub Actions `wave3-smoke` (HSM smoke) | PASS (3 m 19 s) |

**Trust-root surface awareness:** Welle 1 added zero PROTECTED_SURFACE entries because the bridge layer is `apps/`-side, not part of the trust-root surface. The 16 PROTECTED_SURFACE files + 2 PROTECTED_PREFIXES from V1.18 Welle B (8) remain unchanged.

**What landed (one-liner per area):**

- **Bridge layer** — `apps/atlas-web/src/lib/atlas/` (8 new files; ~870 LoC): `event.ts` + `paths.ts` + `signer.ts` + `storage.ts` + `keys.ts` + `schema.ts` + `ulid.ts` + `types.ts`. Per-workspace mutex with race-safe identity-check on delete. Signer subprocess wrapper with timeout + stdout cap. Workspace-path resolution with `existsSync` guard on signer-binary cache.
- **HTTP route** — `apps/atlas-web/src/app/api/atlas/write-node/route.ts` (POST + GET kid-preview, 256 KB Content-Length cap, 4xx/5xx mapping with `redactPaths` on 500).
- **UI** — `apps/atlas-web/src/app/write/page.tsx` (server-rendered host) + `apps/atlas-web/src/components/WriteNodeForm.tsx` (client-interactive form). `layout.tsx` gained `/write` nav link; misleading hardcoded `ws-bankhaus-hagedorn` chip + fake "47s ago" anchor indicator removed.
- **e2e smoke** — `apps/atlas-web/scripts/e2e-write-roundtrip.ts` (form → POST → events.jsonl → `atlas-verify-cli` ✓ VALID, all 9 evidence checks).
- **MCP parity** — `apps/atlas-mcp-server/src/lib/event.ts` got the same Mutex Map-identity-race fix (capture `tail` once; identity-check before delete). Prep for V1.19 Welle 2 bridge extraction.
- **Operator doc** — `docs/OPERATOR-RUNBOOK.md` §17 (NEW): deployer security boundary for the write surface — no-auth rationale, network-gate options (Tailscale / Cloudflare Access / mTLS / IP allowlist), CSRF advisory for cookie-based SSO, rate-limiting advisory (no built-in limiter; Cloudflare Access / nginx `limit_req` / Vercel Edge), failure-mode table.

**How to verify the ship landed correctly (idempotent post-merge probe):**

```bash
# Working tree clean, master at the merge commit:
git status                          # → clean
git log --oneline -3                # → 3853c64 → 6c50699 → d650b5b

# Atlas-web typed + builds + round-trips:
cd apps/atlas-web
pnpm tsc --noEmit                   # → exit 0
pnpm build                          # → exit 0
pnpm e2e:write                      # → ✓ VALID, 9/9 evidence checks
cd ../..

# All four anti-drift gates still green:
bash tools/test-trust-root-mutations.sh        # → 17/17, 18 paths
bash tools/test-tag-signatures.sh              # → 13/13
bash .github/actions/verify-wasm-pin-check/test/run-tests.sh  # → 40/40
bash tools/playground-csp-check.sh             # → PASS
```

**Rollback procedure (if a regression surfaces in Welle 2):**

`3853c64` is a squash-merge — single commit on `master`. Revert via:

```bash
git revert 3853c64                  # creates a clean revert commit; SSH-sign it
git push origin master              # standard REST merge if branch protection requires PR
```

The route handler is gated by `ATLAS_DEV_MASTER_SEED` and any `kid` material lives only inside the Rust signer process — nothing in the Welle 1 ship touches secrets at rest, so revert is trust-safe.

---

## 1. V1.19 Welle 1 — implementation context (rationale, threat model, hardenings)

**State summary:** first user-facing write path. After 18 waves of verify-side and distribution-side hardening (V1.0 → V1.18), V1.19 pivots to product surface. A Browser-User can now create an Atlas node via `POST /api/atlas/write-node` + `/write` page (Next.js 16 App Router, React 19, TS-strict). Trust-Inkrement structurally identical to MCP-host writes — same `atlas-signer` Rust binary, same HKDF-SHA256 workspace-key derivation, same append-only `events.jsonl` format → the Verifier cannot distinguish web-emitted from MCP-emitted events because they're byte-identical at the AtlasEvent envelope layer.

**Threat model & deliberate non-goals (from `route.ts:8-51` source comment):**

1. **NO authentication.** Atlas's trust-model is key-based (every event records `signature.kid`), not user-based. Deployer gates exposure at network/proxy layer — see `OPERATOR-RUNBOOK §17`. Surfacing this route on a public address without an upstream auth gate IS the operator's choice and responsibility. Adding half-baked auth would be worse than none — operators would assume security exists when it's only a thin wrapper.

2. **Per-tenant kid auto-derived from `workspace_id`.** Route NEVER accepts a caller-supplied `kid`. Eliminates "sign as somebody else" affordance the MCP tool intentionally reserves for trusted MCP hosts (Cursor, Claude Desktop). Deterministic via `perTenantKidFor(workspace_id) → "atlas-anchor:{workspace_id}"`.

3. **Single-process locking only.** Per-workspace mutex in `lib/atlas/event.ts` serialises writes within ONE Node process. Running atlas-web AND atlas-mcp-server against the same workspace concurrently CAN fork the DAG. Verifier accepts forks (it's a DAG), but the user's mental model breaks. Deploy one writer per workspace until V2 ships an external lock.

4. **`ATLAS_DEV_MASTER_SEED=1` must be set** in the Next.js process environment (or the wave-3 HSM opt-in), otherwise per-tenant subcommands of `atlas-signer` refuse to start. Surfaced as 500 with a clear message when missing.

**Web-specific hardenings (vs MCP variant):**

- **Body-size + attributes cap (defence-in-depth):** `REQUEST_BODY_MAX_BYTES = 256 KB` (raw HTTP body, Content-Length pre-check before `req.json()` reads stream) + `ATTRIBUTES_MAX_BYTES = 64 KB` (Zod `.refine()` on JSON-serialised attributes). 64 KB is well within Linux ARG_MAX (~2 MB) — payload passes to `atlas-signer` as `--payload <json>` argv string, so 1 MB attributes would E2BIG → generic 500. 64 KB gives clean Zod error at boundary instead.
- **Signer-subprocess timeout + stdout cap:** `SIGN_TIMEOUT_MS = 30_000`, `DERIVE_TIMEOUT_MS = 5_000`, `STDOUT_CAP_BYTES = 512 KB`. Settle-pattern with AbortController-style guard prevents double-resolve on timeout-vs-close race. clearTimeout in both close + error handler.
- **`redactPaths` on 500-responses:** strips absolute filesystem paths from error messages — defends against path-disclosure via 500. Applied to `SignerError.message` and `unexpected: ${err.message}` paths. Defence-in-depth: signer should not write paths to stderr in the first place, but redacting here means we don't depend on that discipline. `StorageError` messages already path-sanitised at source (`storage.ts:sanitiseFsError`).
- **Per-workspace mutex Map-identity-race-closed (CRITICAL pre-resume fix):** `if (workspaceMutex.get(workspaceId) === next) workspaceMutex.delete(workspaceId)` — identity comparison before deletion closes race where concurrent caller already set'd a newer tail and this callback would otherwise delete the newer tail from the map. **Applied to BOTH `apps/atlas-web/src/lib/atlas/event.ts` AND `apps/atlas-mcp-server/src/lib/event.ts`** for parity (and as preparation for V1.19 Welle 2 bridge-extraction).
- **Spread `attributes` FIRST, schema-checked fields win:** `{ ...args.attributes, kind: args.kind, id: args.id }` — never let a caller-supplied attribute object overwrite the schema-checked node identity. Silent override would corrupt the signed payload.
- **`existsSync` guard on `ATLAS_SIGNER_PATH` cache:** if env-var path doesn't exist on disk, fall through to workspace candidates instead of caching an invalid path that would ENOENT-fail every subsequent sign.

---

## 2. Files landed in `3853c64`

**`apps/atlas-web/src/lib/atlas/` (8 files, all NEW — bridge layer):**
| File | Lines | Role |
|---|---|---|
| `event.ts` | 116 | High-level `writeSignedEvent()`; per-workspace mutex with race-safe identity-check on delete |
| `paths.ts` | 120 | Workspace-path resolution + `WorkspacePathError`; `resolveSignerBinary()` with `existsSync` guard |
| `signer.ts` | 278 | atlas-signer subprocess wrapper; `SignerError`; `redactPaths`; `runProcess` timeout + stdout cap |
| `storage.ts` | 97 | events.jsonl append + read; `StorageError` with pre-sanitised messages |
| `keys.ts` | 74 | `perTenantKidFor()` HKDF-derivation; `derivePubkeyViaSigner()` |
| `schema.ts` | 81 | Zod schemas — `WorkspaceIdSchema`, `NodeKindSchema`, `WriteNodeInputSchema` (with 64 KB attributes refine) |
| `ulid.ts` | 66 | event_id generation (ULID; deterministic via seed-injection for tests) |
| `types.ts` | 35 | `AtlasEvent`, `WriteEventResult` |

**`apps/atlas-web/src/app/` (NEW):**
| File | Lines | Role |
|---|---|---|
| `api/atlas/write-node/route.ts` | 222 | POST handler + GET kid-preview; Content-Length 256 KB cap; 4xx/5xx mapping with `redactPaths` on 500 |
| `write/page.tsx` | 59 | Server-rendered + client-interactive form; workspace_id + kind + id + attributes JSON textarea |
| `layout.tsx` | (modified) | Removed hardcoded `ws-bankhaus-hagedorn` chip + fake "last anchor 47s ago" demo placeholder, replaced with explanatory comment block |

**`apps/atlas-mcp-server/src/lib/` (1 file, MODIFIED — parity):**
| File | Lines | Role |
|---|---|---|
| `event.ts` | (modified) | Same Mutex Map-identity-race fix as web variant — `if (mutex.get(id) === next) mutex.delete(id)` |

**`docs/` (1 file, MODIFIED):**
| File | Section | Role |
|---|---|---|
| `OPERATOR-RUNBOOK.md` | §17 | NEW section: operator-boundary for web write-surface. Rate-limiting advisory (no built-in limiter — operator must use Cloudflare WAF / nginx limit_req / Caddy rate_limit). CSRF advisory before rate-limiting (cookie-based SSO scenarios, SameSite=Lax/Strict requirement, Origin/Referer header check in upstream proxy). |

**`apps/atlas-web/scripts/` (1 file, NEW):**
| File | Role |
|---|---|
| `e2e-write-roundtrip.ts` | Round-trip test: invokes the route handler directly with a `Request`, then exports the workspace via the MCP exporter and runs `atlas-verify-cli verify-trace --require-per-tenant-keys` → ✓ VALID with all 9 evidence checks green. Bypasses Next.js HTTP server (the route handler is a pure `(Request) => Promise<NextResponse>`). |

---

## 3. Review-correction record

Two parallel-review iterations; all CRITICAL/HIGH/selected MEDIUMs closed in-commit; e2e ✓ VALID after each iteration.

| Finding | Severity | Source | Fix Location |
|---|---|---|---|
| Mutex Map-Identity Race | **CRITICAL** | code-reviewer (pre-resume) | `event.ts` (web + mcp parity) — identity check before delete |
| Attributes argv overflow → ARG_MAX E2BIG | **HIGH** | security-reviewer (pre-resume) | `schema.ts` 64 KB Zod `.refine()` |
| Unbounded runProcess (signer subprocess could hang or OOM) | MEDIUM | security-reviewer (final) | `signer.ts` 30s/5s timeout + 512 KB stdout cap |
| Missing Content-Length pre-check | LOW | code-reviewer (final) | `route.ts` 256 KB belt-and-braces cap |
| Missing CSRF advisory for cookie-based SSO | LOW | security-reviewer (final) | `OPERATOR-RUNBOOK §17` |
| Missing rate-limiting advisory | LOW | security-reviewer (final) | `OPERATOR-RUNBOOK §17` |

**Severity rationale:**
- **CRITICAL** Mutex Map-Identity Race: silent-bypass of write-serialisation invariant. Concurrent callers could observe the mutex map cleared while another caller's tail was still pending → writes would interleave at the JSONL append layer, potentially corrupting parent-hash chains. Identity-check on delete is the structural fix.
- **HIGH** Attributes argv overflow: not a trust-bypass but a guaranteed crash class for any payload over ~2 MB. The ARG_MAX surface is a Linux kernel limit; `spawn` throws E2BIG, surfacing as a generic 500 that gives no boundary diagnostic. 64 KB Zod refine gives clean error at validation boundary.

---

## 4. V1.19 Welle 2 candidates (deferred LOW findings, no trust impact)

1. **`packages/atlas-bridge/` extraction** — bridge layer is currently `apps/atlas-web/src/lib/atlas/` mirrored against `apps/atlas-mcp-server/src/lib/`. Consolidate as shared workspace package; eliminates drift surface (e.g. `WriteEventResult` kid-field has already diverged between MCP and web variants).
2. **`redactPaths` regex over-eager** — current pattern matches strings that aren't paths (false-positive in error messages). Tighten to require `/` or `[A-Z]:\\` start AND ≥2 path components.
3. **`resolveSignerBinary` cache TTL** — cache lives process-long; binary update between restarts could leave stale path cached. TTL + `existsSync` re-validation.
4. **e2e test dynamic import hoisting** — test file uses dynamic imports; verify Vite hoisting pattern (no current bug, just verification gap).
5. **`ulid()` mutation pattern** — internal pattern violates immutability convention; refactor to pure-function form.
6. **`WriteEventResult` kid-field drift** — MCP and web variants have diverged field shapes; consolidate at bridge extraction (#1).
7. **Playwright E2E coverage** — currently one round-trip test. Edge cases: 4xx paths, large payloads, concurrent writes, workspace_id validation boundaries.

**Operator-side carry-forward from V1.18 (unchanged):**
- `RULESET_VERIFY_TOKEN` PAT configuration per OPERATOR-RUNBOOK §16 (currently `verify-branch-protection` correctly-loud Exit-2 indeterminate, not blocking).
- HSTS-preload submission to `hstspreload.org` after 1 month HTTPS stability.

**Long-horizon (unchanged from V1.18):**
- MCP-Verifier-Hook RFC at Anthropic / MCP-Working-Group.
- Sigstore/gitsign tag-signing as additive backup channel.
- Action consumer-side adoption proof.

---

## 5. Obsidian vault updates this session (parallel to code review)

The Obsidian vault at `C:\Users\nelso\Documents\obsidian-vault\` was updated to current wave:

- **`Projekte/Aktiv/Atlas.md`** — Frontmatter status `v1.19-welle-1-in-flight`; status section now lists V1.19 / V1.18 / V1.17 / V1.16 with cumulative trust-property progression bullet list; build-logs navigation extended with notes 15 + 16.
- **`Projekte/Aktiv/Atlas/13 - Implementation-Journey-V1.0-bis-V1.19.md`** — renamed from V1.16 scope; V-Welle table extended through V1.18 Welle B (8) and V1.19 Welle 1; trust-property progression prose section now covers V1.16 → V1.17 → V1.18 → V1.19 transitions.
- **`Projekte/Aktiv/Atlas/15 - V1.17-und-V1.18-Defence-in-Depth.md`** — NEW deep-dive consolidating V1.17 (3 waves) + V1.18 Welle A + Welle B (1)–(8). Trust-property increment table with adversary-path × wave matrix. Architecture decision rationale (SSH vs GPG vs gitsign; `pull_request` vs `pull_request_target`; Repository Rulesets vs classical branch protection; solo-maintainer `required_approving_review_count: 0`; gating caller in Welle B (6); Exit 2 indeterminate vs Exit 1 drift in Welle B (7)).
- **`Projekte/Aktiv/Atlas/16 - V1.19-Welle-1-Web-Write-Surface.md`** — NEW deep-dive on web write surface. Architecture diagram (Browser → route.ts → writeSignedEvent → signEvent → appendEvent → 200). Threat model. Web-specific hardenings A–F. Reviewer-pass findings table. V1.19 Welle 2 candidates. Operator-boundary section.
- **Backlinks updated** — `Projekte/Aktiv/Atlas/14 - V1.16-Welle-C-Production-Hosting.md` and `Projekte/Aktiv/ADR-Atlas-007-Worker-Emitted-Headers-Plus-Silent-204-Receiver.md` now point at the renamed note 13.
- **ADR-006 namespace collision noted** — vault `ADR-Atlas-006-Workers-Static-Assets-Hosting` (V1.16) is namespace-disjoint from repo `docs/ADR/ADR-Atlas-006-multi-issuer-sigstore-tracking.md` (V1.18). Note 15 references the repo path explicitly to avoid wikilink collision.

> Obsidian-side state survives compaction independently of `.handoff/` files. Either is sufficient for fresh-context bootstrap; the vault is more navigable, the handoff is more dense. Use whichever the next-session agent prefers.

---

## Standing protocol (re-read each session)

1. **Implement** → small, coherent change-set. SSH-sign every commit (key `SHA256:qq/VVJYpsgEdeQSLqU0QS/gKn6ohXJHio+VkzVX+4Zg`).
2. **Parallel review** → spawn `code-reviewer` + `security-reviewer` in single message. Apply CRITICAL + HIGH + selected MEDIUMs in-commit. LOW findings without trust impact go to next-wave handoff.
3. **e2e re-validate** after each fix iteration. Type-check + build clean.
4. **Single feat commit** with full context-block message.
5. **Push + REST merge** via `gh api -X PUT repos/.../pulls/<n>/merge -f merge_method=squash` (no `--admin` override since V1.18 Welle B (3)).
6. **Handoff PR** appending a new `## 0.` section above the prior current section in `.handoff/v1.19-handoff.md`; renumber existing sections downward.

Anti-drift gates (run before any V1.19 Welle 2 edit touching trust-root surfaces — V1.19 Welle 1 itself touches none):
- `bash tools/test-trust-root-mutations.sh` → 17/17, case 7 reports 18 paths
- `bash tools/test-tag-signatures.sh` → 13/13
- `bash .github/actions/verify-wasm-pin-check/test/run-tests.sh` → 40/40
- `bash tools/playground-csp-check.sh` → PASS

Cargo PATH on Windows is non-default — use `/c/Users/nelso/.cargo/bin/cargo.exe`. Repo is Windows host with bash/MSYS; all shell snippets use forward-slash paths.

Trust-root surface awareness: V1.19 Welle 1 added zero PROTECTED_SURFACE entries (bridge layer is `apps/`-side, not part of trust-root surface). The 16 PROTECTED_SURFACE files + 2 PROTECTED_PREFIXES from V1.18 Welle B (8) remain unchanged.
