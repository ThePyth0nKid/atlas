# V1.13 — Agent Handoff

You are continuing the Atlas Verifiable Knowledge Graph project. **V1.12 (operator-surface simplification — `ATLAS_PRODUCTION` removal + ARCHITECTURE.md V1.10/V1.11 sweep + CI lane promotion with new wave-3 smoke + nightly live-Rekor lanes) is complete and on `master`.** Your job is to (a) close the V1.12 carry-over items as a small warm-up sprint (only one LOW deferred), then (b) ship V1.13 — primary scope to be confirmed with the user. The user prefers **German** in chat; respond in German. Concise updates, `TaskCreate` proactively. Standing directive: *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Entscheide du, was jetzt das Bestmögliche ist anhand deiner Informationen, die du hast. Immer beste Sicherheit und beste Codequalität."*

> ⚠️ **Read this in full before touching code.** It captures the V1.12-shipped state, the parallel-review findings deliberately deferred from V1.12, and the V1.13 scope candidates carried forward from V1.12's open list (C/D/E/G/H).

---

## 1. Where we are (snapshot)

### V1.12 commits landed on `master`

V1.12 shipped over 4 commits (chronological, all pushed to `master`):

| Commit | Subject |
|--------|---------|
| `e8d7798` | docs(v1.12/warm-up): operator runbook §2 PIN-file ACL framing + §3 tense polish |
| `f4302b5` | refactor(v1.12/scope-a): remove ATLAS_PRODUCTION env var (V1.9 paranoia layer) |
| `a25b1b7` | ci(v1.12/scope-b1): hsm-byte-equivalence trigger flip + SHA pinning + fork-PR safety |
| `221b503` | feat(v1.12/scope-b2): wave-3 sealed-signer smoke lane + nightly live-Rekor + auto-detect |
| `fda4f79` | docs(v1.12/handoff): record Scope A+B shipped + Scope F obsolete |

### Test status (all green at HEAD on 2026-05-03)

```
cargo test --workspace                           clean (212+ tests)
cargo test -p atlas-signer --features hsm        clean (115+ tests, adds the cryptoki cfg gate)
cargo clippy --all-targets -- -D warnings        clean (default + --features hsm)
pnpm --filter atlas-mcp-server lint              clean
pnpm --filter atlas-mcp-server smoke             ✓ VALID end-to-end (lenient + strict)
```

Smoke negative paths (V1.12 Scope B2 hardening): `ATLAS_HSM_WORKSPACE_SIGNER=1` without trio AND `=true` with partial trio both fail-closed in TS with `(N/3 vars set)` diagnostic before any process spawns.

If any of these is red before you start, **stop and investigate** — V1.12 must remain green.

`cargo` not in default PATH on Windows: use `/c/Users/nelso/.cargo/bin/cargo.exe` or `export PATH="/c/Users/nelso/.cargo/bin:$PATH"` first.

### V1.12 capabilities (shipped)

#### V1.12 warm-up — V1.11 post-review residuals (commit `e8d7798`)

Closes the operator-runbook §2 PIN-file ACL framing finding from the V1.11 wave-3 doc-sync review (the §2 ACL discussion was tense-inconsistent vs §3's wave-3 wording) plus tense polish in §3.

#### V1.12 Scope A — `ATLAS_PRODUCTION` removal + ARCHITECTURE.md V1.10/V1.11 sweep (commit `f4302b5`)

Closed the V1.9-era literal-`"1"` paranoia gate across the entire crate (master_seed_gate, master_seed_loader, workspace_signer_loader, import-ceremony scripts). The V1.10 positive opt-in (`ATLAS_DEV_MASTER_SEED=1`, allow-list `1`/`true`/`yes`/`on` case-insensitive) is now the sole dev-seed gate. The HSM trio (`ATLAS_HSM_PKCS11_LIB` + `ATLAS_HSM_SLOT` + `ATLAS_HSM_PIN_FILE`) is the sole production audit signal. ATLAS_PRODUCTION is silently ignored from V1.12 onwards (no deprecation warning either — that lived during V1.11). ARCHITECTURE.md, SECURITY-NOTES.md, OPERATOR-RUNBOOK.md, and the MCP server README were swept in the same coherent commit. The OPERATOR-RUNBOOK §1 truth tables collapsed from 4→3 rows (master-seed gate) and 5→4 rows (master-seed loader); V1.9 paranoia-layer rows were removed since the runtime no longer admits them.

**Trust property:** the V1.10 positive opt-in covers the same security property as V1.9's negative gate, without the literal-`"1"`-only footgun. A misconfigured deployment fails-closed at the loader rather than silently accepting `ATLAS_PRODUCTION=true` as unset.

#### V1.12 Scope B — CI lane promotion + new wave-3 smoke + nightly Sigstore lanes (commits `a25b1b7` + `221b503`)

Three CI lanes now act as drift sentries for the V1.10 wave-2, V1.11 wave-3, and V1.6+V1.7+V1.8 Sigstore trust properties.

**B1 (`a25b1b7`):** `hsm-byte-equivalence.yml` was promoted from `workflow_dispatch` only to `pull_request:` + `push:` (paths-filtered to signer / trust-core / Cargo.lock / workflow). All `uses:` SHA-pinned (actions/checkout v4.2.2, dtolnay/rust-toolchain stable, Swatinem/rust-cache v2.7.5). PIN moved to `${{ secrets.SOFTHSM2_PIN || '1234' }}` for fork-PR fallback compatibility (fork PRs receive no secrets, the literal protects only a throwaway token created and destroyed in the job). `permissions: contents: read` block added. `set +x` belt-and-suspenders so a future debug toggle (`RUNNER_DEBUG=1`) cannot echo the literal fallback PIN into the run log.

**B2 (`221b503`):**

- `.github/workflows/hsm-wave3-smoke.yml` — full V1.11 wave-3 end-to-end: SoftHSM2 + ATLAS_HSM_* trio + ATLAS_HSM_WORKSPACE_SIGNER=1 → atlas-signer (`--features hsm`) → CKM_EC_EDWARDS_KEY_PAIR_GEN per workspace → CKM_EDDSA(Ed25519) signing inside the token → atlas-mcp-server smoke writes 3 events + 2 anchors → atlas-verify-cli VALID. Same trigger surface as byte-equivalence + TS paths.
- `.github/workflows/sigstore-rekor-nightly.yml` — submits anchor batches to live `rekor.sigstore.dev` at cron `0 6 * * *` UTC + workflow_dispatch. Concurrency-grouped (`group: sigstore-rekor-nightly`, `cancel-in-progress: true`) so a manual dispatch during a scheduled run cancels the older run rather than doubling the production-log footprint. Decoupled from PR triggers so a Sigstore outage cannot block PR turnaround. Uploads trace artefact (14-day retention) for post-mortem.
- `apps/atlas-mcp-server/scripts/smoke.ts` — auto-detects the HSM trio + `ATLAS_HSM_WORKSPACE_SIGNER` opt-in. If complete, skips the dev-seed override; else sets `ATLAS_DEV_MASTER_SEED=1`. Truthy allow-list mirrors the Rust signer (`1`/`true`/`yes`/`on`, case-insensitive, whitespace-tolerant). Partial trio under wave-3 opt-in fails-closed early in TS with `(N/3 vars set)` actionable diagnostic.
- Doc-sync added a "## CI lanes (V1.12 — in scope)" section to SECURITY-NOTES (per-lane trust-property invariants + drift sources, names actual constants `SIGSTORE_REKOR_V1_PEM` for pubkey + `SIGSTORE_REKOR_V1_TREE_IDS` for roster), a `## 8. CI lanes` section to OPERATOR-RUNBOOK (lane-reference table with trigger / invariant / first-look-on-red, failure-handling sketches, "what red lanes do NOT mean" caveats), and a CI-promotion bullet to ARCHITECTURE §V1.12 boundary section. Existing OPERATOR-RUNBOOK §8 Quick Reference moved to §9.

**Trust properties under test:**
- `hsm-byte-equivalence` — V1.10 wave-2 invariant: in-HSM HKDF-SHA256 derivation == host-side derivation for every `(master_seed, workspace_id)` pair.
- `hsm-wave3-smoke` — V1.11 wave-3 invariant: per-workspace scalar never enters Atlas address space; signatures verify against pubkeys advertised in the bundle.
- `sigstore-rekor-nightly` — V1.6+V1.7+V1.8 Sigstore stack invariant: live anchor submission + inclusion-proof verification against the pinned ECDSA P-256 log pubkey + active shard roster.

#### Doc state after V1.12

- `docs/OPERATOR-RUNBOOK.md` — header V1.12. New §8 CI lanes (was §8 Quick Reference, now §9). §1 truth tables collapsed (V1.9 paranoia rows removed).
- `docs/SECURITY-NOTES.md` — header V1.12. New "## CI lanes (V1.12 — in scope)" section between V1.11 wave-3 residual risks and Crate boundary.
- `docs/ARCHITECTURE.md` — header V1.12. New §V1.12 boundary section. §10 V1.9 "Production gate" bullet annotated with V1.10 supersession + V1.12 removal. §V1.10 + §V1.11 boundary sections added (filled the prior gap where ARCHITECTURE was stuck at V1.9 header).
- `apps/atlas-mcp-server/README.md` — V1.10 positive opt-in semantics documented; ATLAS_PRODUCTION references removed.

---

## 2. V1.12 post-review carry-overs — V1.13 warm-up (light)

The V1.12 Scope B commit was reviewed by parallel `code-reviewer` + `security-reviewer` agents. All HIGH and MEDIUM findings were addressed in-commit. Only one LOW remains as carry-over.

### LOW — drop in passing during warm-up

1. **Workflow comment expansion: `softhsm2-util --init-token` `/proc/cmdline` exposure.** Both `hsm-byte-equivalence.yml` and `hsm-wave3-smoke.yml` have a `set +x` guard with an inline comment explaining the shell-trace defence. The security reviewer noted that the comment doesn't acknowledge the orthogonal `/proc/<pid>/cmdline` exposure of `softhsm2-util --init-token` (which the OPERATOR-RUNBOOK §2 covers in detail). A future editor reading only the workflow comment might not realise the cmdline exposure exists and could remove `set +x` thinking it's the only PIN-leak path. Add a single-line note to both workflow comments referencing the runbook §2 PIN-argv discussion. Safe to drop in passing during V1.13 warm-up; no security regression in the meantime (CI runner is single-tenant, exposure window is bounded to one-shot init).

That's the entire V1.12 carry-over list. The auto-detect partial-trio fail-fast, the allow-list alignment, the SECURITY-NOTES constant-name fix, the wave-3 caveat in OPERATOR-RUNBOOK §8 — all addressed in-commit.

---

## 3. V1.13 — primary scope candidates

V1.12 shipped operator-surface simplification + CI drift sentries. V1.13 has freedom to choose between several directions, all carried forward from the V1.12 trade-off table; confirm scope with the user before writing code.

### Trade-off table

| Scope | Time-to-ship | Trust impact | Operator visibility |
|-------|--------------|--------------|---------------------|
| **C) Witness cosignature wiring (real, not stub)** | ~2 sessions | High — second independent attestor over chain head | New trace field; auditor sees ✓ witnessed |
| **D)** Multi-issuer Sigstore redundancy | ~2 sessions | Medium — Rekor-shard roster expansion + dual-issuance | Trace field; verifier accepts either |
| **E)** WASM verifier publishing (npm package) | ~1 session | Low — same trust, broader consumption | Browser/Node consumers get a ready binary |
| **G)** Cedar policy enforcement | ~3+ sessions | High — policy-gated writes | Major surface change; arguably V2-scope |
| **H)** FalkorDB / Postgres storage migration | ~3+ sessions | Low — operational, not trust | Storage backend swap |

**Recommendation: C as primary.** Witness cosignature is the highest-trust-impact scope still in V1-territory (G is V2-scope; H is operational, not trust). It pairs cleanly with V1.12's Scope B nightly Sigstore lane: a witness over the chain head adds a second independent attestor that the auditor can verify without trusting Atlas itself. The witness-stub scaffolding may already exist in `crates/atlas-trust-core/src/anchor.rs` (verify before relying on this — the V1.12 handoff §3 mentioned witness as candidate C, which suggests prior-art exists). If C ships in 2 sessions, a third session can pair it with E (WASM verifier publishing) for a "V1.13 = trust + reach" story.

If the user prefers a faster-impact cycle, **E alone** (WASM verifier as published npm package) is a one-session win with low risk and broader consumption surface.

If user picks D/G/H instead, follow their lead and rewrite §3 inline.

### Implementation seams (use what V1.11 + V1.12 left in place)

For **C (Witness cosignature):**
- Search `crates/atlas-trust-core/src/anchor.rs` for any `Witness` / `witness` field on `AnchorBatch` — V1.11/V1.12 may have left a stub.
- The pattern mirrors V1.7 anchor-chain tip-rotation: an additional signature over the chain head, verified against a second pinned key. The shard-roster mechanism in V1.7 (`SIGSTORE_REKOR_V1_TREE_IDS`) is the precedent for "pinned roster of accepted attestors".
- A real witness needs an attestor identity (witness pubkey) + a signature scheme (Ed25519 likely, or Sigstore-style ECDSA P-256 if reusing the Sigstore stack) + a verification rule (how many witnesses required for VALID; majority? all? configurable?).
- Operator surface: where does the witness live? Co-located with atlas-signer? Separate sidecar binary? External service called via HTTP? This is the largest design question for C.

For **E (WASM verifier publishing):**
- `crates/atlas-verify-wasm/` already exists. The publishing surface is `wasm-pack build --target web` (or `--target nodejs`) + `npm publish`.
- The MCP server (`apps/atlas-mcp-server/`) likely already imports the WASM build for in-process verification — check its `package.json` for the dependency shape.
- npm publication needs a scope (`@atlas-trust/verify-wasm`?), package metadata (license = Apache-2.0 per crate boundary), and CI to publish on tag push.
- Bonus: a tiny browser-based verifier playground would surface "drop a trace.json + bundle.json, click verify, see the result" — high-leverage demo with low engineering cost.

### Backwards compatibility

- **C:** Adding a witness field to traces is backwards-compatible if older traces simply have no witness (verifier accepts in lenient mode, requires in strict mode behind a `--require-witness` flag — same pattern as V1.9 per-tenant keys).
- **E:** No backwards-compat impact; this is a publishing/distribution change.

---

## 4. Footguns / hard-won learnings

### Carried forward (still load-bearing in V1.13)

1. **`anchor-chain.jsonl` is append-only and irreversible** — sole writer is the issuer. (V1.7)
2. **Chain head canonicalisation must be byte-identical between issuer and verifier.** Pinned golden tests. (V1.7)
3. **Verifier may NOT trust `trace.anchor_chain.head` field** — recompute. (V1.7)
4. **Sigstore Rekor v1 shard roster is a source change** — adding tree-IDs requires crate version bump. (V1.7)
5. **Every signer-stdout boundary in TS uses `parseAnchorJson`** (lossless-json). (V1.8)
6. **`cargo` not in default PATH on Windows** — `/c/Users/nelso/.cargo/bin/cargo.exe`.
7. **Smoke at `apps/atlas-mcp-server`** — `pnpm --filter atlas-mcp-server smoke` from root.
8. **Single canonicalisation source — TS never re-implements anything cryptographic.** Always shell to `atlas-signer`. (V1.5+)
9. **HKDF info parameter is the trust boundary for per-tenant key separation.** (V1.9)
10. **Backward compat lenient mode is a trust trade-off, not a free win.** Strict mode is the security boundary. (V1.9)
11. **Don't re-derive a pubkey at verify time.** Verifier reads from the bundle. (V1.9)
12. **`std::env::set_var` in tests races on multi-core CI.** Use the V1.10 injection-form gates. (V1.10)
13. **Cargo no-cycles rule trumps clean module decomposition.** HSM lives in `crates/atlas-signer/src/hsm/` behind `--features hsm`, not in a sibling crate. (V1.10)
14. **PKCS#11 is verbose; cryptoki abstracts the syntax but not the semantics.** Test against SoftHSM2 with pinned-pubkey goldens before trusting any vendor's HKDF/EDDSA. (V1.10)
15. **Drop order matters for FFI resources.** `Mutex<Session>` BEFORE `_ctx: Pkcs11` so `C_CloseSession` runs before `C_Finalize`. (V1.10)
16. **Absolute-path guard at config-parse time, not at use time.** Platform-conditional test data on Windows vs Unix. (V1.10)
17. **Zeroize at every heap-resident copy of secret material.** PIN bytes, HKDF output, per-tenant seed array — all wrapped. (V1.10)
18. **Operator runbook ceremonies must use secrets-manager env vars, not literal PINs.** `umask 077`, source from secrets manager, `unset` at end. **wave-3 ceremony (V1.11) further upgrades this to `--pin-source env:VAR` for `pkcs11-tool` so even argv exposure is closed.** (V1.10/V1.11)
19. **Pre-flight test work pays for itself.** The V1.11 byte-equivalence test was instrumental in committing to wave-3's "explicit opt-in because pubkeys rotate" design. (V1.11)
20. **Trait dyn-safety is non-trivial and worth pinning explicitly.** `WorkspaceSigner` is `Send + Sync` + dyn-safe — no `async fn`. (V1.11)
21. **Layered fail-closed dispatchers need explicit refusal text per layer.** Each refusal site names the env var to fix. (V1.11)
22. **`derive-key` style "structurally refused" subcommands are a real defensive pattern.** Refuse at the CLI surface, not at the FFI layer. (V1.11)
23. **PIN-on-argv is unavoidable on some legacy CLIs (softhsm2-util's init-token).** Document honestly + recommend host-hardening. Don't conflate shell history with `/proc/<pid>/cmdline`. (V1.11)
24. **Section renumbering in long runbooks is reversible if anchors are textual, not numeric.** Prefer textual anchors over numeric in cross-reference comments. (V1.11)

### NEW for V1.13 (V1.12 lessons)

25. **TS-side env-var allow-lists must mirror the Rust side exactly.** When TS reads an env var that gates a Rust dispatcher, mirror the Rust truthy allow-list (`1`/`true`/`yes`/`on`, case-insensitive). A strict `=== "1"` check in TS while Rust accepts `true` produces a silent mode-mismatch where Rust activates one layer and TS logs a different layer — defeats audit signals. The `isTruthyEnv` helper in `apps/atlas-mcp-server/scripts/smoke.ts:65` is the load-bearing example. (V1.12 Scope B2)

26. **Fail-closed early at the closest boundary to the misconfiguration.** When wave-3 is opted in but the trio is partial, the Rust signer correctly refuses — but the TS smoke now also refuses, with a `(N/3 vars set)` diagnostic, BEFORE spawning any process. The closer the failure to the source of the misconfiguration, the cheaper the operator's debug loop. (V1.12 Scope B2 — `smoke.ts` partial-trio guard)

27. **CI lane promotion has a paths-filter discipline.** When promoting `workflow_dispatch`-only lanes to `pull_request:`, list every directory whose changes can affect the lane's behaviour — but no more. Over-broad filters waste CI cost and trigger spurious red on doc-only PRs. Under-broad filters miss real drift. The V1.12 Scope B1+B2 paths-filters are the reference: `crates/atlas-signer/**`, `crates/atlas-trust-core/**`, `Cargo.lock`, the workflow file itself, plus TS paths for the smoke lane. Test-only file changes are deliberately excluded — those are low-risk. (V1.12 Scope B1+B2)

28. **Fork-PR safety needs both a PIN fallback AND `permissions: contents: read`.** GitHub does not inject `secrets.*` into pull_request events from forks; the `${{ secrets.X || 'default' }}` pattern keeps the lane functional, but the literal default is not auto-masked, so `set +x` is the load-bearing PIN-leak guard. The `permissions: contents: read` block bounds the GITHUB_TOKEN scope so a fork-PR-injected workflow change cannot escalate to write capability. **Both must be present together** — neither alone is sufficient. (V1.12 Scope B1)

29. **Live-API CI lanes must be decoupled from PR triggers.** A nightly cron + manual dispatch is the right shape for any lane that hits an upstream API (Sigstore, etc.) where an outage on their side would otherwise block every PR. Concurrency group with `cancel-in-progress: true` prevents doubled submission if a manual dispatch races a scheduled run. (V1.12 Scope B2 — `sigstore-rekor-nightly.yml`)

---

## 5. Repo layout cheat-sheet (V1.12-shipped state)

```
atlas/
├─ crates/
│  ├─ atlas-trust-core/           # verifier core, anchor + bundle types
│  │  ├─ src/anchor.rs            # SIGSTORE_REKOR_V1_PEM + TREE_IDS roster
│  │  ├─ src/event/sig.rs         # Ed25519 signature acceptance
│  │  └─ tests/
│  │     ├─ sigstore_golden.rs    # pinned shard goldens
│  │     └─ anchor_chain_adversary.rs
│  ├─ atlas-signer/               # signing CLI + HSM backend
│  │  ├─ src/keys.rs              # master_seed_gate (V1.10 positive opt-in only after V1.12)
│  │  ├─ src/workspace_signer.rs  # WorkspaceSigner trait + 3-layer dispatcher
│  │  ├─ src/hsm/                 # PKCS#11 backend (--features hsm)
│  │  │  ├─ pkcs11.rs             # master-seed wave-2 sealed-seed loader
│  │  │  └─ pkcs11_workspace.rs   # wave-3 sealed signer + handle cache (line 159)
│  │  ├─ src/main.rs              # CLI surface + derive-key wave-3 refusal
│  │  └─ tests/hsm_byte_equivalence.rs  # V1.10 wave-2 invariant test
│  ├─ atlas-verify-cli/           # default-features verifier
│  └─ atlas-verify-wasm/          # WASM verifier (V1.13 candidate E)
├─ apps/
│  └─ atlas-mcp-server/           # TS MCP server
│     ├─ scripts/smoke.ts         # E2E smoke (V1.12 wave-3 auto-detect at line 65)
│     └─ src/tools/anchor-bundle.ts  # ATLAS_REKOR_URL dispatch
├─ .github/workflows/
│  ├─ hsm-byte-equivalence.yml    # V1.12 B1: PR + push, paths-filtered, SHA-pinned
│  ├─ hsm-wave3-smoke.yml         # V1.12 B2: full wave-3 E2E
│  └─ sigstore-rekor-nightly.yml  # V1.12 B2: live-Rekor nightly
├─ docs/
│  ├─ ARCHITECTURE.md             # header V1.12, §V1.10/V1.11/V1.12 boundaries
│  ├─ SECURITY-NOTES.md           # header V1.12, "CI lanes" section
│  └─ OPERATOR-RUNBOOK.md         # header V1.12, §8 CI lanes + §9 Quick Ref
└─ .handoff/
   ├─ v1.6-phase2-handoff.md
   ├─ v1.7-anchor-chain-handoff.md
   ├─ v1.8-handoff.md
   ├─ v1.9-handoff.md
   ├─ v1.10-handoff.md
   ├─ v1.11-handoff.md
   ├─ v1.12-handoff.md            # forensic; do not act on
   └─ v1.13-handoff.md            # ← you are here
```

---

## 6. Workflow for the next session

1. **Read this file in full.** Then read in this order, depending on chosen scope:
   - All scopes: confirm V1.12 baseline green (test status §1).
   - Scope C: `crates/atlas-trust-core/src/anchor.rs` (look for any existing `witness` / `Witness` field), `docs/SECURITY-NOTES.md` (V1.7 shard roster pattern as precedent).
   - Scope E: `crates/atlas-verify-wasm/`, `apps/atlas-mcp-server/package.json` (existing WASM consumer shape).
   - Carry-over LOW: `.github/workflows/hsm-byte-equivalence.yml` + `hsm-wave3-smoke.yml` (the comment expansion target).

2. **Confirm baseline green** — run all suites + clippy (default + `--features hsm`) + smoke. Stop if anything is red.

3. **Use `TaskCreate`** to track V1.13 sub-tasks. The warm-up is one LOW; the primary scope is its own task tree.

4. **Settle scope question (§3) with user FIRST** before coding. Recommended: C; confirm before starting.

5. **TDD discipline.** For scope C: write failing tests for the witness verification path (lenient accept missing, strict reject missing) before wiring the witness signature scheme. For scope E: smoke the WASM build against the existing trust-core test vectors.

6. **Single coherent commit per unit** — keep changes separable for review (the V1.12 B1/B2 split is the reference shape).

7. **After implementation but before commit:** run smoke + tests + parallel `code-reviewer` + `security-reviewer`. Address all CRITICAL/HIGH before commit. MEDIUMs when diff cost is low.

8. **Doc-sync is the closing act of every feature commit cluster.** ARCHITECTURE.md, SECURITY-NOTES.md, OPERATOR-RUNBOOK.md, MCP-server README. Use `doc-updater` agent for routine pieces; review residual-risk paragraphs manually.

### User-collaboration notes

- The user prefers **German** in chat. Concise updates. `TaskCreate` proactively.
- The user has approved (V1.5–V1.12) the pattern of "implement → review with parallel agents → fix findings → commit". Do this without asking.
- The user tolerates session compactions — that's why these handoff docs exist. **Always update `.handoff/` if you make a meaningful design decision.**
- Standing directive: *"Komplett einmal reviewen und dann commit."* Parallel `code-reviewer` + `security-reviewer`. Address CRITICAL/HIGH before commit. MEDIUMs when diff cost is low.
- Standing directive (V1.5+): *"Geh mit A und hol es bestmöglich aus dem Produkt raus. Immer beste Sicherheit und beste Codequalität."* When in doubt, the more security-hardening choice wins, but never at the cost of operator ergonomics.
- The user is comfortable with you choosing among design options as long as you make trade-offs explicit. They'll redirect if your call doesn't match their priority.

---

## 7. What "done" looks like

V1.13 done is scope-dependent; baseline expectations:

```
$ cargo test --workspace               (default features)
… 212+ ok …  (V1.12 baseline + V1.13 additions)

$ cargo test -p atlas-signer --features hsm
… 115+ ok …

$ cargo clippy --all-targets -- -D warnings    (both feature configs)
… clean …

$ pnpm --filter atlas-mcp-server smoke
[smoke] HSM-backed signer (or dev opt-in)
[smoke] verify (strict)             ✓ VALID — strict mode
                                    ✓ per-tenant-keys
                                    ✓ anchor-chain
                                    [✓ witnessed if scope C shipped]
```

If scope C (Witness cosignature) shipped:

```
$ atlas-verify-cli --strict --require-witness trace.json bundle.json
✓ VALID — all checks passed (strict mode)
  ✓ witness — signed by 1 of 1 required attestors
```

If scope E (WASM verifier publishing) shipped:

```
$ npm install @atlas-trust/verify-wasm
$ node -e "import('@atlas-trust/verify-wasm').then(m => m.verify(trace, bundle))"
{ valid: true, evidence: {...} }
```

Plus: docs updated (ARCHITECTURE adds §V1.13 boundary; OPERATOR-RUNBOOK adds witness-attestor ceremony if scope C; SECURITY-NOTES adds witness threat-model section if scope C), V1.12 carry-over LOW closed where touched, all tests green, clippy clean, code-reviewer + security-reviewer findings addressed.

V1.13 explicitly does NOT include (deferred):
- Cedar policy enforcement (V2)
- Full COSE_Sign1 (V2)
- SPIFFE attestation (V2)
- Graph-database integration (V2)
- Per-workspace token isolation (V2; out-of-scope per V1.11 SECURITY-NOTES)
- Multi-token redundancy under wave-3 (architecturally incompatible; not a V1.x item)

If the user pulls any forward: discuss scope explicitly, write a fresh handoff if it shifts mid-session, ship in separable commits.

Ship it.
